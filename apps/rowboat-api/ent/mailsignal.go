// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/mailsignal"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/mailthread"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/user"
	"github.com/google/uuid"
)

// MailSignal is the model entity for the MailSignal schema.
type MailSignal struct {
	config `json:"-"`
	// ID of the ent.
	ID uuid.UUID `json:"id,omitempty"`
	// CreatedAt holds the value of the "created_at" field.
	CreatedAt time.Time `json:"created_at,omitempty"`
	// UpdatedAt holds the value of the "updated_at" field.
	UpdatedAt time.Time `json:"updated_at,omitempty"`
	// Classification holds the value of the "classification" field.
	Classification string `json:"classification,omitempty"`
	// Summary holds the value of the "summary" field.
	Summary string `json:"-"`
	// EmbeddingModel holds the value of the "embedding_model" field.
	EmbeddingModel string `json:"embedding_model,omitempty"`
	// Embedding holds the value of the "embedding" field.
	Embedding []byte `json:"-"`
	// ComputedAt holds the value of the "computed_at" field.
	ComputedAt time.Time `json:"computed_at,omitempty"`
	// Edges holds the relations/edges for other nodes in the graph.
	// The values are being populated by the MailSignalQuery when eager-loading is set.
	Edges             MailSignalEdges `json:"edges"`
	mail_thread_id    *uuid.UUID
	user_mail_signals *uuid.UUID
	selectValues      sql.SelectValues
}

// MailSignalEdges holds the relations/edges for other nodes in the graph.
type MailSignalEdges struct {
	// Thread holds the value of the thread edge.
	Thread *MailThread `json:"thread,omitempty"`
	// User holds the value of the user edge.
	User *User `json:"user,omitempty"`
	// loadedTypes holds the information for reporting if a
	// type was loaded (or requested) in eager-loading or not.
	loadedTypes [2]bool
	// totalCount holds the count of the edges above.
	totalCount [2]map[string]int
}

// ThreadOrErr returns the Thread value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MailSignalEdges) ThreadOrErr() (*MailThread, error) {
	if e.Thread != nil {
		return e.Thread, nil
	} else if e.loadedTypes[0] {
		return nil, &NotFoundError{label: mailthread.Label}
	}
	return nil, &NotLoadedError{edge: "thread"}
}

// UserOrErr returns the User value or an error if the edge
// was not loaded in eager-loading, or loaded but was not found.
func (e MailSignalEdges) UserOrErr() (*User, error) {
	if e.User != nil {
		return e.User, nil
	} else if e.loadedTypes[1] {
		return nil, &NotFoundError{label: user.Label}
	}
	return nil, &NotLoadedError{edge: "user"}
}

// scanValues returns the types for scanning values from sql.Rows.
func (*MailSignal) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case mailsignal.FieldEmbedding:
			values[i] = new([]byte)
		case mailsignal.FieldClassification, mailsignal.FieldSummary, mailsignal.FieldEmbeddingModel:
			values[i] = new(sql.NullString)
		case mailsignal.FieldCreatedAt, mailsignal.FieldUpdatedAt, mailsignal.FieldComputedAt:
			values[i] = new(sql.NullTime)
		case mailsignal.FieldID:
			values[i] = new(uuid.UUID)
		case mailsignal.ForeignKeys[0]: // mail_thread_id
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		case mailsignal.ForeignKeys[1]: // user_mail_signals
			values[i] = &sql.NullScanner{S: new(uuid.UUID)}
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the MailSignal fields.
func (_m *MailSignal) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case mailsignal.FieldID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field id", values[i])
			} else if value != nil {
				_m.ID = *value
			}
		case mailsignal.FieldCreatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field created_at", values[i])
			} else if value.Valid {
				_m.CreatedAt = value.Time
			}
		case mailsignal.FieldUpdatedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field updated_at", values[i])
			} else if value.Valid {
				_m.UpdatedAt = value.Time
			}
		case mailsignal.FieldClassification:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field classification", values[i])
			} else if value.Valid {
				_m.Classification = value.String
			}
		case mailsignal.FieldSummary:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field summary", values[i])
			} else if value.Valid {
				_m.Summary = value.String
			}
		case mailsignal.FieldEmbeddingModel:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field embedding_model", values[i])
			} else if value.Valid {
				_m.EmbeddingModel = value.String
			}
		case mailsignal.FieldEmbedding:
			if value, ok := values[i].(*[]byte); !ok {
				return fmt.Errorf("unexpected type %T for field embedding", values[i])
			} else if value != nil {
				_m.Embedding = *value
			}
		case mailsignal.FieldComputedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field computed_at", values[i])
			} else if value.Valid {
				_m.ComputedAt = value.Time
			}
		case mailsignal.ForeignKeys[0]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field mail_thread_id", values[i])
			} else if value.Valid {
				_m.mail_thread_id = new(uuid.UUID)
				*_m.mail_thread_id = *value.S.(*uuid.UUID)
			}
		case mailsignal.ForeignKeys[1]:
			if value, ok := values[i].(*sql.NullScanner); !ok {
				return fmt.Errorf("unexpected type %T for field user_mail_signals", values[i])
			} else if value.Valid {
				_m.user_mail_signals = new(uuid.UUID)
				*_m.user_mail_signals = *value.S.(*uuid.UUID)
			}
		default:
			_m.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the MailSignal.
// This includes values selected through modifiers, order, etc.
func (_m *MailSignal) Value(name string) (ent.Value, error) {
	return _m.selectValues.Get(name)
}

// QueryThread queries the "thread" edge of the MailSignal entity.
func (_m *MailSignal) QueryThread() *MailThreadQuery {
	return NewMailSignalClient(_m.config).QueryThread(_m)
}

// QueryUser queries the "user" edge of the MailSignal entity.
func (_m *MailSignal) QueryUser() *UserQuery {
	return NewMailSignalClient(_m.config).QueryUser(_m)
}

// Update returns a builder for updating this MailSignal.
// Note that you need to call MailSignal.Unwrap() before calling this method if this MailSignal
// was returned from a transaction, and the transaction was committed or rolled back.
func (_m *MailSignal) Update() *MailSignalUpdateOne {
	return NewMailSignalClient(_m.config).UpdateOne(_m)
}

// Unwrap unwraps the MailSignal entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (_m *MailSignal) Unwrap() *MailSignal {
	_tx, ok := _m.config.driver.(*txDriver)
	if !ok {
		panic("ent: MailSignal is not a transactional entity")
	}
	_m.config.driver = _tx.drv
	return _m
}

// String implements the fmt.Stringer.
func (_m *MailSignal) String() string {
	var builder strings.Builder
	builder.WriteString("MailSignal(")
	builder.WriteString(fmt.Sprintf("id=%v, ", _m.ID))
	builder.WriteString("created_at=")
	builder.WriteString(_m.CreatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("updated_at=")
	builder.WriteString(_m.UpdatedAt.Format(time.ANSIC))
	builder.WriteString(", ")
	builder.WriteString("classification=")
	builder.WriteString(_m.Classification)
	builder.WriteString(", ")
	builder.WriteString("summary=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("embedding_model=")
	builder.WriteString(_m.EmbeddingModel)
	builder.WriteString(", ")
	builder.WriteString("embedding=<sensitive>")
	builder.WriteString(", ")
	builder.WriteString("computed_at=")
	builder.WriteString(_m.ComputedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// MailSignals is a parsable slice of MailSignal.
type MailSignals []*MailSignal

// Code generated by enthistory, DO NOT EDIT.

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"errors"
	"fmt"
	"time"

	"entgo.io/ent"

	"github.com/flume/enthistory"
)

var (
	idNotFoundError = errors.New("could not get id from mutation")
)

func EntOpToHistoryOp(op ent.Op) enthistory.OpType {
	switch op {
	case ent.OpDelete, ent.OpDeleteOne:
		return enthistory.OpTypeDelete
	case ent.OpUpdate, ent.OpUpdateOne:
		return enthistory.OpTypeUpdate
	default:
		return enthistory.OpTypeInsert
	}
}

func rollback(tx *Tx, err error) error {
	if tx != nil {
		if rerr := tx.Rollback(); rerr != nil {
			err = fmt.Errorf("%w: %v", err, rerr)
		}
		return err
	}
	return err
}

func (m *AgentDefinitionMutation) CreateHistoryFromCreate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	id, ok := m.ID()
	if !ok {
		return rollback(tx, idNotFoundError)
	}

	create := client.AgentDefinitionHistory.Create()
	if tx != nil {
		create = tx.AgentDefinitionHistory.Create()
	}

	create = create.
		SetOperation(EntOpToHistoryOp(m.Op())).
		SetHistoryTime(time.Now()).
		SetRef(id)

	if createdAt, exists := m.CreatedAt(); exists {
		create = create.SetCreatedAt(createdAt)
	}

	if updatedAt, exists := m.UpdatedAt(); exists {
		create = create.SetUpdatedAt(updatedAt)
	}

	if slug, exists := m.Slug(); exists {
		create = create.SetSlug(slug)
	}

	if name, exists := m.Name(); exists {
		create = create.SetName(name)
	}

	if instructions, exists := m.Instructions(); exists {
		create = create.SetInstructions(instructions)
	}

	if model, exists := m.Model(); exists {
		create = create.SetModel(model)
	}

	if provider, exists := m.Provider(); exists {
		create = create.SetProvider(provider)
	}

	if limitsJSON, exists := m.LimitsJSON(); exists {
		create = create.SetLimitsJSON(limitsJSON)
	}

	if enabledTools, exists := m.EnabledTools(); exists {
		create = create.SetEnabledTools(enabledTools)
	}

	if toolsJSON, exists := m.ToolsJSON(); exists {
		create = create.SetToolsJSON(toolsJSON)
	}

	if subagentRefs, exists := m.SubagentRefs(); exists {
		create = create.SetSubagentRefs(subagentRefs)
	}

	if channelBindings, exists := m.ChannelBindings(); exists {
		create = create.SetChannelBindings(channelBindings)
	}

	if connectorReqs, exists := m.ConnectorReqs(); exists {
		create = create.SetConnectorReqs(connectorReqs)
	}

	if source, exists := m.Source(); exists {
		create = create.SetSource(source)
	}

	if forkedFrom, exists := m.ForkedFrom(); exists {
		create = create.SetForkedFrom(forkedFrom)
	}

	if revision, exists := m.Revision(); exists {
		create = create.SetRevision(revision)
	}

	if sourceFormat, exists := m.SourceFormat(); exists {
		create = create.SetSourceFormat(sourceFormat)
	}

	if rawSource, exists := m.RawSource(); exists {
		create = create.SetRawSource(rawSource)
	}

	if contentHash, exists := m.ContentHash(); exists {
		create = create.SetContentHash(contentHash)
	}

	if managedBy, exists := m.ManagedBy(); exists {
		create = create.SetManagedBy(managedBy)
	}

	if agentSyncState, exists := m.AgentSyncState(); exists {
		create = create.SetAgentSyncState(agentSyncState)
	}

	if agentSyncError, exists := m.AgentSyncError(); exists {
		create = create.SetAgentSyncError(agentSyncError)
	}

	_, err = create.Save(ctx)
	if err != nil {
		rollback(tx, err)
	}
	return nil
}

func (m *AgentDefinitionMutation) CreateHistoryFromUpdate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		agentdefinition, err := client.AgentDefinition.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.AgentDefinitionHistory.Create()
		if tx != nil {
			create = tx.AgentDefinitionHistory.Create()
		}

		create = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id)

		if createdAt, exists := m.CreatedAt(); exists {
			create = create.SetCreatedAt(createdAt)
		} else {
			create = create.SetCreatedAt(agentdefinition.CreatedAt)
		}

		if updatedAt, exists := m.UpdatedAt(); exists {
			create = create.SetUpdatedAt(updatedAt)
		} else {
			create = create.SetUpdatedAt(agentdefinition.UpdatedAt)
		}

		if slug, exists := m.Slug(); exists {
			create = create.SetSlug(slug)
		} else {
			create = create.SetSlug(agentdefinition.Slug)
		}

		if name, exists := m.Name(); exists {
			create = create.SetName(name)
		} else {
			create = create.SetName(agentdefinition.Name)
		}

		if instructions, exists := m.Instructions(); exists {
			create = create.SetInstructions(instructions)
		} else {
			create = create.SetInstructions(agentdefinition.Instructions)
		}

		if model, exists := m.Model(); exists {
			create = create.SetModel(model)
		} else {
			create = create.SetModel(agentdefinition.Model)
		}

		if provider, exists := m.Provider(); exists {
			create = create.SetProvider(provider)
		} else {
			create = create.SetProvider(agentdefinition.Provider)
		}

		if limitsJSON, exists := m.LimitsJSON(); exists {
			create = create.SetLimitsJSON(limitsJSON)
		} else {
			create = create.SetLimitsJSON(agentdefinition.LimitsJSON)
		}

		if enabledTools, exists := m.EnabledTools(); exists {
			create = create.SetEnabledTools(enabledTools)
		} else {
			create = create.SetEnabledTools(agentdefinition.EnabledTools)
		}

		if toolsJSON, exists := m.ToolsJSON(); exists {
			create = create.SetToolsJSON(toolsJSON)
		} else {
			create = create.SetToolsJSON(agentdefinition.ToolsJSON)
		}

		if subagentRefs, exists := m.SubagentRefs(); exists {
			create = create.SetSubagentRefs(subagentRefs)
		} else {
			create = create.SetSubagentRefs(agentdefinition.SubagentRefs)
		}

		if channelBindings, exists := m.ChannelBindings(); exists {
			create = create.SetChannelBindings(channelBindings)
		} else {
			create = create.SetChannelBindings(agentdefinition.ChannelBindings)
		}

		if connectorReqs, exists := m.ConnectorReqs(); exists {
			create = create.SetConnectorReqs(connectorReqs)
		} else {
			create = create.SetConnectorReqs(agentdefinition.ConnectorReqs)
		}

		if source, exists := m.Source(); exists {
			create = create.SetSource(source)
		} else {
			create = create.SetSource(agentdefinition.Source)
		}

		if forkedFrom, exists := m.ForkedFrom(); exists {
			create = create.SetForkedFrom(forkedFrom)
		} else {
			create = create.SetForkedFrom(agentdefinition.ForkedFrom)
		}

		if revision, exists := m.Revision(); exists {
			create = create.SetRevision(revision)
		} else {
			create = create.SetRevision(agentdefinition.Revision)
		}

		if sourceFormat, exists := m.SourceFormat(); exists {
			create = create.SetSourceFormat(sourceFormat)
		} else {
			create = create.SetSourceFormat(agentdefinition.SourceFormat)
		}

		if rawSource, exists := m.RawSource(); exists {
			create = create.SetRawSource(rawSource)
		} else {
			create = create.SetRawSource(agentdefinition.RawSource)
		}

		if contentHash, exists := m.ContentHash(); exists {
			create = create.SetContentHash(contentHash)
		} else {
			create = create.SetContentHash(agentdefinition.ContentHash)
		}

		if managedBy, exists := m.ManagedBy(); exists {
			create = create.SetManagedBy(managedBy)
		} else {
			create = create.SetManagedBy(agentdefinition.ManagedBy)
		}

		if agentSyncState, exists := m.AgentSyncState(); exists {
			create = create.SetAgentSyncState(agentSyncState)
		} else {
			create = create.SetAgentSyncState(agentdefinition.AgentSyncState)
		}

		if agentSyncError, exists := m.AgentSyncError(); exists {
			create = create.SetAgentSyncError(agentSyncError)
		} else {
			create = create.SetAgentSyncError(agentdefinition.AgentSyncError)
		}

		_, err = create.Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *AgentDefinitionMutation) CreateHistoryFromDelete(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		agentdefinition, err := client.AgentDefinition.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.AgentDefinitionHistory.Create()
		if tx != nil {
			create = tx.AgentDefinitionHistory.Create()
		}

		_, err = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id).
			SetCreatedAt(agentdefinition.CreatedAt).
			SetUpdatedAt(agentdefinition.UpdatedAt).
			SetSlug(agentdefinition.Slug).
			SetName(agentdefinition.Name).
			SetInstructions(agentdefinition.Instructions).
			SetModel(agentdefinition.Model).
			SetProvider(agentdefinition.Provider).
			SetLimitsJSON(agentdefinition.LimitsJSON).
			SetEnabledTools(agentdefinition.EnabledTools).
			SetToolsJSON(agentdefinition.ToolsJSON).
			SetSubagentRefs(agentdefinition.SubagentRefs).
			SetChannelBindings(agentdefinition.ChannelBindings).
			SetConnectorReqs(agentdefinition.ConnectorReqs).
			SetSource(agentdefinition.Source).
			SetForkedFrom(agentdefinition.ForkedFrom).
			SetRevision(agentdefinition.Revision).
			SetSourceFormat(agentdefinition.SourceFormat).
			SetRawSource(agentdefinition.RawSource).
			SetContentHash(agentdefinition.ContentHash).
			SetManagedBy(agentdefinition.ManagedBy).
			SetAgentSyncState(agentdefinition.AgentSyncState).
			SetAgentSyncError(agentdefinition.AgentSyncError).
			Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *LLMUsageMutation) CreateHistoryFromCreate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	id, ok := m.ID()
	if !ok {
		return rollback(tx, idNotFoundError)
	}

	create := client.LLMUsageHistory.Create()
	if tx != nil {
		create = tx.LLMUsageHistory.Create()
	}

	create = create.
		SetOperation(EntOpToHistoryOp(m.Op())).
		SetHistoryTime(time.Now()).
		SetRef(id)

	if model, exists := m.Model(); exists {
		create = create.SetModel(model)
	}

	if useCase, exists := m.UseCase(); exists {
		create = create.SetUseCase(useCase)
	}

	if subUseCase, exists := m.SubUseCase(); exists {
		create = create.SetSubUseCase(subUseCase)
	}

	if agentName, exists := m.AgentName(); exists {
		create = create.SetAgentName(agentName)
	}

	if inputTokens, exists := m.InputTokens(); exists {
		create = create.SetInputTokens(inputTokens)
	}

	if outputTokens, exists := m.OutputTokens(); exists {
		create = create.SetOutputTokens(outputTokens)
	}

	if costUnits, exists := m.CostUnits(); exists {
		create = create.SetCostUnits(costUnits)
	}

	if requestID, exists := m.RequestID(); exists {
		create = create.SetRequestID(requestID)
	}

	if ts, exists := m.Ts(); exists {
		create = create.SetTs(ts)
	}

	_, err = create.Save(ctx)
	if err != nil {
		rollback(tx, err)
	}
	return nil
}

func (m *LLMUsageMutation) CreateHistoryFromUpdate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		llmusage, err := client.LLMUsage.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.LLMUsageHistory.Create()
		if tx != nil {
			create = tx.LLMUsageHistory.Create()
		}

		create = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id)

		if model, exists := m.Model(); exists {
			create = create.SetModel(model)
		} else {
			create = create.SetModel(llmusage.Model)
		}

		if useCase, exists := m.UseCase(); exists {
			create = create.SetUseCase(useCase)
		} else {
			create = create.SetUseCase(llmusage.UseCase)
		}

		if subUseCase, exists := m.SubUseCase(); exists {
			create = create.SetSubUseCase(subUseCase)
		} else {
			create = create.SetSubUseCase(llmusage.SubUseCase)
		}

		if agentName, exists := m.AgentName(); exists {
			create = create.SetAgentName(agentName)
		} else {
			create = create.SetAgentName(llmusage.AgentName)
		}

		if inputTokens, exists := m.InputTokens(); exists {
			create = create.SetInputTokens(inputTokens)
		} else {
			create = create.SetInputTokens(llmusage.InputTokens)
		}

		if outputTokens, exists := m.OutputTokens(); exists {
			create = create.SetOutputTokens(outputTokens)
		} else {
			create = create.SetOutputTokens(llmusage.OutputTokens)
		}

		if costUnits, exists := m.CostUnits(); exists {
			create = create.SetCostUnits(costUnits)
		} else {
			create = create.SetCostUnits(llmusage.CostUnits)
		}

		if requestID, exists := m.RequestID(); exists {
			create = create.SetRequestID(requestID)
		} else {
			create = create.SetRequestID(llmusage.RequestID)
		}

		if ts, exists := m.Ts(); exists {
			create = create.SetTs(ts)
		} else {
			create = create.SetTs(llmusage.Ts)
		}

		_, err = create.Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *LLMUsageMutation) CreateHistoryFromDelete(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		llmusage, err := client.LLMUsage.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.LLMUsageHistory.Create()
		if tx != nil {
			create = tx.LLMUsageHistory.Create()
		}

		_, err = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id).
			SetModel(llmusage.Model).
			SetUseCase(llmusage.UseCase).
			SetSubUseCase(llmusage.SubUseCase).
			SetAgentName(llmusage.AgentName).
			SetInputTokens(llmusage.InputTokens).
			SetOutputTokens(llmusage.OutputTokens).
			SetCostUnits(llmusage.CostUnits).
			SetRequestID(llmusage.RequestID).
			SetTs(llmusage.Ts).
			Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *MCPConnectionMutation) CreateHistoryFromCreate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	id, ok := m.ID()
	if !ok {
		return rollback(tx, idNotFoundError)
	}

	create := client.MCPConnectionHistory.Create()
	if tx != nil {
		create = tx.MCPConnectionHistory.Create()
	}

	create = create.
		SetOperation(EntOpToHistoryOp(m.Op())).
		SetHistoryTime(time.Now()).
		SetRef(id)

	if createdAt, exists := m.CreatedAt(); exists {
		create = create.SetCreatedAt(createdAt)
	}

	if updatedAt, exists := m.UpdatedAt(); exists {
		create = create.SetUpdatedAt(updatedAt)
	}

	if connector, exists := m.Connector(); exists {
		create = create.SetConnector(connector)
	}

	if audience, exists := m.Audience(); exists {
		create = create.SetAudience(audience)
	}

	if organizationID, exists := m.OrganizationID(); exists {
		create = create.SetOrganizationID(organizationID)
	}

	if scopes, exists := m.Scopes(); exists {
		create = create.SetScopes(scopes)
	}

	if refreshTokenPresent, exists := m.RefreshTokenPresent(); exists {
		create = create.SetRefreshTokenPresent(refreshTokenPresent)
	}

	if apiKeyPresent, exists := m.APIKeyPresent(); exists {
		create = create.SetAPIKeyPresent(apiKeyPresent)
	}

	if credentialGeneration, exists := m.CredentialGeneration(); exists {
		create = create.SetCredentialGeneration(credentialGeneration)
	}

	if status, exists := m.Status(); exists {
		create = create.SetStatus(status)
	}

	if connectedAt, exists := m.ConnectedAt(); exists {
		create = create.SetConnectedAt(connectedAt)
	}

	if lastUsedAt, exists := m.LastUsedAt(); exists {
		create = create.SetLastUsedAt(lastUsedAt)
	}

	if expiresAt, exists := m.ExpiresAt(); exists {
		create = create.SetExpiresAt(expiresAt)
	}

	if revokedAt, exists := m.RevokedAt(); exists {
		create = create.SetRevokedAt(revokedAt)
	}

	if revokedReason, exists := m.RevokedReason(); exists {
		create = create.SetRevokedReason(revokedReason)
	}

	if revokedBy, exists := m.RevokedBy(); exists {
		create = create.SetRevokedBy(revokedBy)
	}

	if revocationAttemptedAt, exists := m.RevocationAttemptedAt(); exists {
		create = create.SetRevocationAttemptedAt(revocationAttemptedAt)
	}

	if revocationSucceeded, exists := m.RevocationSucceeded(); exists {
		create = create.SetRevocationSucceeded(revocationSucceeded)
	}

	_, err = create.Save(ctx)
	if err != nil {
		rollback(tx, err)
	}
	return nil
}

func (m *MCPConnectionMutation) CreateHistoryFromUpdate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		mcpconnection, err := client.MCPConnection.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.MCPConnectionHistory.Create()
		if tx != nil {
			create = tx.MCPConnectionHistory.Create()
		}

		create = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id)

		if createdAt, exists := m.CreatedAt(); exists {
			create = create.SetCreatedAt(createdAt)
		} else {
			create = create.SetCreatedAt(mcpconnection.CreatedAt)
		}

		if updatedAt, exists := m.UpdatedAt(); exists {
			create = create.SetUpdatedAt(updatedAt)
		} else {
			create = create.SetUpdatedAt(mcpconnection.UpdatedAt)
		}

		if connector, exists := m.Connector(); exists {
			create = create.SetConnector(connector)
		} else {
			create = create.SetConnector(mcpconnection.Connector)
		}

		if audience, exists := m.Audience(); exists {
			create = create.SetAudience(audience)
		} else {
			create = create.SetAudience(mcpconnection.Audience)
		}

		if organizationID, exists := m.OrganizationID(); exists {
			create = create.SetOrganizationID(organizationID)
		} else {
			create = create.SetOrganizationID(mcpconnection.OrganizationID)
		}

		if scopes, exists := m.Scopes(); exists {
			create = create.SetScopes(scopes)
		} else {
			create = create.SetScopes(mcpconnection.Scopes)
		}

		if refreshTokenPresent, exists := m.RefreshTokenPresent(); exists {
			create = create.SetRefreshTokenPresent(refreshTokenPresent)
		} else {
			create = create.SetRefreshTokenPresent(mcpconnection.RefreshTokenPresent)
		}

		if apiKeyPresent, exists := m.APIKeyPresent(); exists {
			create = create.SetAPIKeyPresent(apiKeyPresent)
		} else {
			create = create.SetAPIKeyPresent(mcpconnection.APIKeyPresent)
		}

		if credentialGeneration, exists := m.CredentialGeneration(); exists {
			create = create.SetCredentialGeneration(credentialGeneration)
		} else {
			create = create.SetCredentialGeneration(mcpconnection.CredentialGeneration)
		}

		if status, exists := m.Status(); exists {
			create = create.SetStatus(status)
		} else {
			create = create.SetStatus(mcpconnection.Status)
		}

		if connectedAt, exists := m.ConnectedAt(); exists {
			create = create.SetConnectedAt(connectedAt)
		} else {
			create = create.SetConnectedAt(mcpconnection.ConnectedAt)
		}

		if lastUsedAt, exists := m.LastUsedAt(); exists {
			create = create.SetLastUsedAt(lastUsedAt)
		} else {
			create = create.SetLastUsedAt(mcpconnection.LastUsedAt)
		}

		if expiresAt, exists := m.ExpiresAt(); exists {
			create = create.SetExpiresAt(expiresAt)
		} else {
			create = create.SetExpiresAt(mcpconnection.ExpiresAt)
		}

		if revokedAt, exists := m.RevokedAt(); exists {
			create = create.SetRevokedAt(revokedAt)
		} else {
			create = create.SetRevokedAt(mcpconnection.RevokedAt)
		}

		if revokedReason, exists := m.RevokedReason(); exists {
			create = create.SetRevokedReason(revokedReason)
		} else {
			create = create.SetRevokedReason(mcpconnection.RevokedReason)
		}

		if revokedBy, exists := m.RevokedBy(); exists {
			create = create.SetRevokedBy(revokedBy)
		} else {
			create = create.SetRevokedBy(mcpconnection.RevokedBy)
		}

		if revocationAttemptedAt, exists := m.RevocationAttemptedAt(); exists {
			create = create.SetRevocationAttemptedAt(revocationAttemptedAt)
		} else {
			create = create.SetRevocationAttemptedAt(mcpconnection.RevocationAttemptedAt)
		}

		if revocationSucceeded, exists := m.RevocationSucceeded(); exists {
			create = create.SetRevocationSucceeded(revocationSucceeded)
		} else {
			create = create.SetRevocationSucceeded(mcpconnection.RevocationSucceeded)
		}

		_, err = create.Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *MCPConnectionMutation) CreateHistoryFromDelete(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		mcpconnection, err := client.MCPConnection.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.MCPConnectionHistory.Create()
		if tx != nil {
			create = tx.MCPConnectionHistory.Create()
		}

		_, err = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id).
			SetCreatedAt(mcpconnection.CreatedAt).
			SetUpdatedAt(mcpconnection.UpdatedAt).
			SetConnector(mcpconnection.Connector).
			SetAudience(mcpconnection.Audience).
			SetOrganizationID(mcpconnection.OrganizationID).
			SetScopes(mcpconnection.Scopes).
			SetRefreshTokenPresent(mcpconnection.RefreshTokenPresent).
			SetAPIKeyPresent(mcpconnection.APIKeyPresent).
			SetCredentialGeneration(mcpconnection.CredentialGeneration).
			SetStatus(mcpconnection.Status).
			SetConnectedAt(mcpconnection.ConnectedAt).
			SetLastUsedAt(mcpconnection.LastUsedAt).
			SetExpiresAt(mcpconnection.ExpiresAt).
			SetRevokedAt(mcpconnection.RevokedAt).
			SetRevokedReason(mcpconnection.RevokedReason).
			SetRevokedBy(mcpconnection.RevokedBy).
			SetRevocationAttemptedAt(mcpconnection.RevocationAttemptedAt).
			SetRevocationSucceeded(mcpconnection.RevocationSucceeded).
			Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *OAuthConnectionMutation) CreateHistoryFromCreate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	id, ok := m.ID()
	if !ok {
		return rollback(tx, idNotFoundError)
	}

	create := client.OAuthConnectionHistory.Create()
	if tx != nil {
		create = tx.OAuthConnectionHistory.Create()
	}

	create = create.
		SetOperation(EntOpToHistoryOp(m.Op())).
		SetHistoryTime(time.Now()).
		SetRef(id)

	if createdAt, exists := m.CreatedAt(); exists {
		create = create.SetCreatedAt(createdAt)
	}

	if updatedAt, exists := m.UpdatedAt(); exists {
		create = create.SetUpdatedAt(updatedAt)
	}

	if provider, exists := m.Provider(); exists {
		create = create.SetProvider(provider)
	}

	if refreshTokenPresent, exists := m.RefreshTokenPresent(); exists {
		create = create.SetRefreshTokenPresent(refreshTokenPresent)
	}

	if credentialGeneration, exists := m.CredentialGeneration(); exists {
		create = create.SetCredentialGeneration(credentialGeneration)
	}

	if scopes, exists := m.Scopes(); exists {
		create = create.SetScopes(scopes)
	}

	if externalAccountID, exists := m.ExternalAccountID(); exists {
		create = create.SetExternalAccountID(externalAccountID)
	}

	_, err = create.Save(ctx)
	if err != nil {
		rollback(tx, err)
	}
	return nil
}

func (m *OAuthConnectionMutation) CreateHistoryFromUpdate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		oauthconnection, err := client.OAuthConnection.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.OAuthConnectionHistory.Create()
		if tx != nil {
			create = tx.OAuthConnectionHistory.Create()
		}

		create = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id)

		if createdAt, exists := m.CreatedAt(); exists {
			create = create.SetCreatedAt(createdAt)
		} else {
			create = create.SetCreatedAt(oauthconnection.CreatedAt)
		}

		if updatedAt, exists := m.UpdatedAt(); exists {
			create = create.SetUpdatedAt(updatedAt)
		} else {
			create = create.SetUpdatedAt(oauthconnection.UpdatedAt)
		}

		if provider, exists := m.Provider(); exists {
			create = create.SetProvider(provider)
		} else {
			create = create.SetProvider(oauthconnection.Provider)
		}

		if refreshTokenPresent, exists := m.RefreshTokenPresent(); exists {
			create = create.SetRefreshTokenPresent(refreshTokenPresent)
		} else {
			create = create.SetRefreshTokenPresent(oauthconnection.RefreshTokenPresent)
		}

		if credentialGeneration, exists := m.CredentialGeneration(); exists {
			create = create.SetCredentialGeneration(credentialGeneration)
		} else {
			create = create.SetCredentialGeneration(oauthconnection.CredentialGeneration)
		}

		if scopes, exists := m.Scopes(); exists {
			create = create.SetScopes(scopes)
		} else {
			create = create.SetScopes(oauthconnection.Scopes)
		}

		if externalAccountID, exists := m.ExternalAccountID(); exists {
			create = create.SetExternalAccountID(externalAccountID)
		} else {
			create = create.SetExternalAccountID(oauthconnection.ExternalAccountID)
		}

		_, err = create.Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *OAuthConnectionMutation) CreateHistoryFromDelete(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		oauthconnection, err := client.OAuthConnection.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.OAuthConnectionHistory.Create()
		if tx != nil {
			create = tx.OAuthConnectionHistory.Create()
		}

		_, err = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id).
			SetCreatedAt(oauthconnection.CreatedAt).
			SetUpdatedAt(oauthconnection.UpdatedAt).
			SetProvider(oauthconnection.Provider).
			SetRefreshTokenPresent(oauthconnection.RefreshTokenPresent).
			SetCredentialGeneration(oauthconnection.CredentialGeneration).
			SetScopes(oauthconnection.Scopes).
			SetExternalAccountID(oauthconnection.ExternalAccountID).
			Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *SubscriptionMutation) CreateHistoryFromCreate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	id, ok := m.ID()
	if !ok {
		return rollback(tx, idNotFoundError)
	}

	create := client.SubscriptionHistory.Create()
	if tx != nil {
		create = tx.SubscriptionHistory.Create()
	}

	create = create.
		SetOperation(EntOpToHistoryOp(m.Op())).
		SetHistoryTime(time.Now()).
		SetRef(id)

	if createdAt, exists := m.CreatedAt(); exists {
		create = create.SetCreatedAt(createdAt)
	}

	if updatedAt, exists := m.UpdatedAt(); exists {
		create = create.SetUpdatedAt(updatedAt)
	}

	if plan, exists := m.Plan(); exists {
		create = create.SetPlan(plan)
	}

	if status, exists := m.Status(); exists {
		create = create.SetStatus(status)
	}

	if trialExpiresAt, exists := m.TrialExpiresAt(); exists {
		create = create.SetNillableTrialExpiresAt(&trialExpiresAt)
	}

	if sanctionedCredits, exists := m.SanctionedCredits(); exists {
		create = create.SetSanctionedCredits(sanctionedCredits)
	}

	if stripeCustomerID, exists := m.StripeCustomerID(); exists {
		create = create.SetStripeCustomerID(stripeCustomerID)
	}

	if stripeSubscriptionID, exists := m.StripeSubscriptionID(); exists {
		create = create.SetStripeSubscriptionID(stripeSubscriptionID)
	}

	_, err = create.Save(ctx)
	if err != nil {
		rollback(tx, err)
	}
	return nil
}

func (m *SubscriptionMutation) CreateHistoryFromUpdate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		subscription, err := client.Subscription.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.SubscriptionHistory.Create()
		if tx != nil {
			create = tx.SubscriptionHistory.Create()
		}

		create = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id)

		if createdAt, exists := m.CreatedAt(); exists {
			create = create.SetCreatedAt(createdAt)
		} else {
			create = create.SetCreatedAt(subscription.CreatedAt)
		}

		if updatedAt, exists := m.UpdatedAt(); exists {
			create = create.SetUpdatedAt(updatedAt)
		} else {
			create = create.SetUpdatedAt(subscription.UpdatedAt)
		}

		if plan, exists := m.Plan(); exists {
			create = create.SetPlan(plan)
		} else {
			create = create.SetPlan(subscription.Plan)
		}

		if status, exists := m.Status(); exists {
			create = create.SetStatus(status)
		} else {
			create = create.SetStatus(subscription.Status)
		}

		if trialExpiresAt, exists := m.TrialExpiresAt(); exists {
			create = create.SetNillableTrialExpiresAt(&trialExpiresAt)
		} else if !m.TrialExpiresAtCleared() {
			create = create.SetNillableTrialExpiresAt(subscription.TrialExpiresAt)
		}

		if sanctionedCredits, exists := m.SanctionedCredits(); exists {
			create = create.SetSanctionedCredits(sanctionedCredits)
		} else {
			create = create.SetSanctionedCredits(subscription.SanctionedCredits)
		}

		if stripeCustomerID, exists := m.StripeCustomerID(); exists {
			create = create.SetStripeCustomerID(stripeCustomerID)
		} else {
			create = create.SetStripeCustomerID(subscription.StripeCustomerID)
		}

		if stripeSubscriptionID, exists := m.StripeSubscriptionID(); exists {
			create = create.SetStripeSubscriptionID(stripeSubscriptionID)
		} else {
			create = create.SetStripeSubscriptionID(subscription.StripeSubscriptionID)
		}

		_, err = create.Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *SubscriptionMutation) CreateHistoryFromDelete(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		subscription, err := client.Subscription.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.SubscriptionHistory.Create()
		if tx != nil {
			create = tx.SubscriptionHistory.Create()
		}

		_, err = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id).
			SetCreatedAt(subscription.CreatedAt).
			SetUpdatedAt(subscription.UpdatedAt).
			SetPlan(subscription.Plan).
			SetStatus(subscription.Status).
			SetNillableTrialExpiresAt(subscription.TrialExpiresAt).
			SetSanctionedCredits(subscription.SanctionedCredits).
			SetStripeCustomerID(subscription.StripeCustomerID).
			SetStripeSubscriptionID(subscription.StripeSubscriptionID).
			Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *UserMutation) CreateHistoryFromCreate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	id, ok := m.ID()
	if !ok {
		return rollback(tx, idNotFoundError)
	}

	create := client.UserHistory.Create()
	if tx != nil {
		create = tx.UserHistory.Create()
	}

	create = create.
		SetOperation(EntOpToHistoryOp(m.Op())).
		SetHistoryTime(time.Now()).
		SetRef(id)

	if createdAt, exists := m.CreatedAt(); exists {
		create = create.SetCreatedAt(createdAt)
	}

	if updatedAt, exists := m.UpdatedAt(); exists {
		create = create.SetUpdatedAt(updatedAt)
	}

	if email, exists := m.Email(); exists {
		create = create.SetEmail(email)
	}

	if workosUserID, exists := m.WorkosUserID(); exists {
		create = create.SetWorkosUserID(workosUserID)
	}

	if workosOrgID, exists := m.WorkosOrgID(); exists {
		create = create.SetWorkosOrgID(workosOrgID)
	}

	_, err = create.Save(ctx)
	if err != nil {
		rollback(tx, err)
	}
	return nil
}

func (m *UserMutation) CreateHistoryFromUpdate(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		user, err := client.User.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.UserHistory.Create()
		if tx != nil {
			create = tx.UserHistory.Create()
		}

		create = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id)

		if createdAt, exists := m.CreatedAt(); exists {
			create = create.SetCreatedAt(createdAt)
		} else {
			create = create.SetCreatedAt(user.CreatedAt)
		}

		if updatedAt, exists := m.UpdatedAt(); exists {
			create = create.SetUpdatedAt(updatedAt)
		} else {
			create = create.SetUpdatedAt(user.UpdatedAt)
		}

		if email, exists := m.Email(); exists {
			create = create.SetEmail(email)
		} else {
			create = create.SetEmail(user.Email)
		}

		if workosUserID, exists := m.WorkosUserID(); exists {
			create = create.SetWorkosUserID(workosUserID)
		} else {
			create = create.SetWorkosUserID(user.WorkosUserID)
		}

		if workosOrgID, exists := m.WorkosOrgID(); exists {
			create = create.SetWorkosOrgID(workosOrgID)
		} else {
			create = create.SetWorkosOrgID(user.WorkosOrgID)
		}

		_, err = create.Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

func (m *UserMutation) CreateHistoryFromDelete(ctx context.Context) error {
	client := m.Client()
	tx, err := m.Tx()
	if err != nil {
		tx = nil
	}

	ids, err := m.IDs(ctx)
	if err != nil {
		return rollback(tx, fmt.Errorf("getting ids: %w", err))
	}

	for _, id := range ids {
		user, err := client.User.Get(ctx, id)
		if err != nil {
			return rollback(tx, err)
		}

		create := client.UserHistory.Create()
		if tx != nil {
			create = tx.UserHistory.Create()
		}

		_, err = create.
			SetOperation(EntOpToHistoryOp(m.Op())).
			SetHistoryTime(time.Now()).
			SetRef(id).
			SetCreatedAt(user.CreatedAt).
			SetUpdatedAt(user.UpdatedAt).
			SetEmail(user.Email).
			SetWorkosUserID(user.WorkosUserID).
			SetWorkosOrgID(user.WorkosOrgID).
			Save(ctx)
		if err != nil {
			rollback(tx, err)
		}
	}

	return nil
}

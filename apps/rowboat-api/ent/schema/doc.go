// Package schema defines the ent schemas — entities, fields, edges and
// indexes — from which the typed client in the parent ent package is
// generated. Hooks, interceptors and tenant-scoping policies are registered
// at the client level in internal/db (see that package for why).
package schema

// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"database/sql/driver"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/commitment"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/commitmentdependency"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/commitmentevent"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/predicate"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/relationship"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/revenueevidence"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/revenueworkspace"
	"github.com/Oppulence-Engineering/rowboat/apps/rowboat-api/ent/user"
	"github.com/google/uuid"
)

// CommitmentQuery is the builder for querying Commitment entities.
type CommitmentQuery struct {
	config
	ctx                           *QueryContext
	order                         []commitment.OrderOption
	inters                        []Interceptor
	predicates                    []predicate.Commitment
	withWorkspace                 *RevenueWorkspaceQuery
	withRelationship              *RelationshipQuery
	withUser                      *UserQuery
	withEvidences                 *RevenueEvidenceQuery
	withEvents                    *CommitmentEventQuery
	withOutgoingDependencies      *CommitmentDependencyQuery
	withIncomingDependencies      *CommitmentDependencyQuery
	withFKs                       bool
	modifiers                     []func(*sql.Selector)
	loadTotal                     []func(context.Context, []*Commitment) error
	withNamedEvidences            map[string]*RevenueEvidenceQuery
	withNamedEvents               map[string]*CommitmentEventQuery
	withNamedOutgoingDependencies map[string]*CommitmentDependencyQuery
	withNamedIncomingDependencies map[string]*CommitmentDependencyQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the CommitmentQuery builder.
func (_q *CommitmentQuery) Where(ps ...predicate.Commitment) *CommitmentQuery {
	_q.predicates = append(_q.predicates, ps...)
	return _q
}

// Limit the number of records to be returned by this query.
func (_q *CommitmentQuery) Limit(limit int) *CommitmentQuery {
	_q.ctx.Limit = &limit
	return _q
}

// Offset to start from.
func (_q *CommitmentQuery) Offset(offset int) *CommitmentQuery {
	_q.ctx.Offset = &offset
	return _q
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (_q *CommitmentQuery) Unique(unique bool) *CommitmentQuery {
	_q.ctx.Unique = &unique
	return _q
}

// Order specifies how the records should be ordered.
func (_q *CommitmentQuery) Order(o ...commitment.OrderOption) *CommitmentQuery {
	_q.order = append(_q.order, o...)
	return _q
}

// QueryWorkspace chains the current query on the "workspace" edge.
func (_q *CommitmentQuery) QueryWorkspace() *RevenueWorkspaceQuery {
	query := (&RevenueWorkspaceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(commitment.Table, commitment.FieldID, selector),
			sqlgraph.To(revenueworkspace.Table, revenueworkspace.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, commitment.WorkspaceTable, commitment.WorkspaceColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryRelationship chains the current query on the "relationship" edge.
func (_q *CommitmentQuery) QueryRelationship() *RelationshipQuery {
	query := (&RelationshipClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(commitment.Table, commitment.FieldID, selector),
			sqlgraph.To(relationship.Table, relationship.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, commitment.RelationshipTable, commitment.RelationshipColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryUser chains the current query on the "user" edge.
func (_q *CommitmentQuery) QueryUser() *UserQuery {
	query := (&UserClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(commitment.Table, commitment.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, commitment.UserTable, commitment.UserColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEvidences chains the current query on the "evidences" edge.
func (_q *CommitmentQuery) QueryEvidences() *RevenueEvidenceQuery {
	query := (&RevenueEvidenceClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(commitment.Table, commitment.FieldID, selector),
			sqlgraph.To(revenueevidence.Table, revenueevidence.FieldID),
			sqlgraph.Edge(sqlgraph.M2M, false, commitment.EvidencesTable, commitment.EvidencesPrimaryKey...),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryEvents chains the current query on the "events" edge.
func (_q *CommitmentQuery) QueryEvents() *CommitmentEventQuery {
	query := (&CommitmentEventClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(commitment.Table, commitment.FieldID, selector),
			sqlgraph.To(commitmentevent.Table, commitmentevent.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, commitment.EventsTable, commitment.EventsColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryOutgoingDependencies chains the current query on the "outgoing_dependencies" edge.
func (_q *CommitmentQuery) QueryOutgoingDependencies() *CommitmentDependencyQuery {
	query := (&CommitmentDependencyClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(commitment.Table, commitment.FieldID, selector),
			sqlgraph.To(commitmentdependency.Table, commitmentdependency.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, commitment.OutgoingDependenciesTable, commitment.OutgoingDependenciesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QueryIncomingDependencies chains the current query on the "incoming_dependencies" edge.
func (_q *CommitmentQuery) QueryIncomingDependencies() *CommitmentDependencyQuery {
	query := (&CommitmentDependencyClient{config: _q.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := _q.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := _q.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(commitment.Table, commitment.FieldID, selector),
			sqlgraph.To(commitmentdependency.Table, commitmentdependency.FieldID),
			sqlgraph.Edge(sqlgraph.O2M, false, commitment.IncomingDependenciesTable, commitment.IncomingDependenciesColumn),
		)
		fromU = sqlgraph.SetNeighbors(_q.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first Commitment entity from the query.
// Returns a *NotFoundError when no Commitment was found.
func (_q *CommitmentQuery) First(ctx context.Context) (*Commitment, error) {
	nodes, err := _q.Limit(1).All(setContextOp(ctx, _q.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{commitment.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (_q *CommitmentQuery) FirstX(ctx context.Context) *Commitment {
	node, err := _q.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first Commitment ID from the query.
// Returns a *NotFoundError when no Commitment ID was found.
func (_q *CommitmentQuery) FirstID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(1).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{commitment.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (_q *CommitmentQuery) FirstIDX(ctx context.Context) uuid.UUID {
	id, err := _q.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single Commitment entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one Commitment entity is found.
// Returns a *NotFoundError when no Commitment entities are found.
func (_q *CommitmentQuery) Only(ctx context.Context) (*Commitment, error) {
	nodes, err := _q.Limit(2).All(setContextOp(ctx, _q.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{commitment.Label}
	default:
		return nil, &NotSingularError{commitment.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (_q *CommitmentQuery) OnlyX(ctx context.Context) *Commitment {
	node, err := _q.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only Commitment ID in the query.
// Returns a *NotSingularError when more than one Commitment ID is found.
// Returns a *NotFoundError when no entities are found.
func (_q *CommitmentQuery) OnlyID(ctx context.Context) (id uuid.UUID, err error) {
	var ids []uuid.UUID
	if ids, err = _q.Limit(2).IDs(setContextOp(ctx, _q.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{commitment.Label}
	default:
		err = &NotSingularError{commitment.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (_q *CommitmentQuery) OnlyIDX(ctx context.Context) uuid.UUID {
	id, err := _q.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of Commitments.
func (_q *CommitmentQuery) All(ctx context.Context) ([]*Commitment, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryAll)
	if err := _q.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*Commitment, *CommitmentQuery]()
	return withInterceptors[[]*Commitment](ctx, _q, qr, _q.inters)
}

// AllX is like All, but panics if an error occurs.
func (_q *CommitmentQuery) AllX(ctx context.Context) []*Commitment {
	nodes, err := _q.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of Commitment IDs.
func (_q *CommitmentQuery) IDs(ctx context.Context) (ids []uuid.UUID, err error) {
	if _q.ctx.Unique == nil && _q.path != nil {
		_q.Unique(true)
	}
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryIDs)
	if err = _q.Select(commitment.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (_q *CommitmentQuery) IDsX(ctx context.Context) []uuid.UUID {
	ids, err := _q.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (_q *CommitmentQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryCount)
	if err := _q.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, _q, querierCount[*CommitmentQuery](), _q.inters)
}

// CountX is like Count, but panics if an error occurs.
func (_q *CommitmentQuery) CountX(ctx context.Context) int {
	count, err := _q.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (_q *CommitmentQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, _q.ctx, ent.OpQueryExist)
	switch _, err := _q.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (_q *CommitmentQuery) ExistX(ctx context.Context) bool {
	exist, err := _q.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the CommitmentQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (_q *CommitmentQuery) Clone() *CommitmentQuery {
	if _q == nil {
		return nil
	}
	return &CommitmentQuery{
		config:                   _q.config,
		ctx:                      _q.ctx.Clone(),
		order:                    append([]commitment.OrderOption{}, _q.order...),
		inters:                   append([]Interceptor{}, _q.inters...),
		predicates:               append([]predicate.Commitment{}, _q.predicates...),
		withWorkspace:            _q.withWorkspace.Clone(),
		withRelationship:         _q.withRelationship.Clone(),
		withUser:                 _q.withUser.Clone(),
		withEvidences:            _q.withEvidences.Clone(),
		withEvents:               _q.withEvents.Clone(),
		withOutgoingDependencies: _q.withOutgoingDependencies.Clone(),
		withIncomingDependencies: _q.withIncomingDependencies.Clone(),
		// clone intermediate query.
		sql:  _q.sql.Clone(),
		path: _q.path,
	}
}

// WithWorkspace tells the query-builder to eager-load the nodes that are connected to
// the "workspace" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithWorkspace(opts ...func(*RevenueWorkspaceQuery)) *CommitmentQuery {
	query := (&RevenueWorkspaceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withWorkspace = query
	return _q
}

// WithRelationship tells the query-builder to eager-load the nodes that are connected to
// the "relationship" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithRelationship(opts ...func(*RelationshipQuery)) *CommitmentQuery {
	query := (&RelationshipClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withRelationship = query
	return _q
}

// WithUser tells the query-builder to eager-load the nodes that are connected to
// the "user" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithUser(opts ...func(*UserQuery)) *CommitmentQuery {
	query := (&UserClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withUser = query
	return _q
}

// WithEvidences tells the query-builder to eager-load the nodes that are connected to
// the "evidences" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithEvidences(opts ...func(*RevenueEvidenceQuery)) *CommitmentQuery {
	query := (&RevenueEvidenceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEvidences = query
	return _q
}

// WithEvents tells the query-builder to eager-load the nodes that are connected to
// the "events" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithEvents(opts ...func(*CommitmentEventQuery)) *CommitmentQuery {
	query := (&CommitmentEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withEvents = query
	return _q
}

// WithOutgoingDependencies tells the query-builder to eager-load the nodes that are connected to
// the "outgoing_dependencies" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithOutgoingDependencies(opts ...func(*CommitmentDependencyQuery)) *CommitmentQuery {
	query := (&CommitmentDependencyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withOutgoingDependencies = query
	return _q
}

// WithIncomingDependencies tells the query-builder to eager-load the nodes that are connected to
// the "incoming_dependencies" edge. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithIncomingDependencies(opts ...func(*CommitmentDependencyQuery)) *CommitmentQuery {
	query := (&CommitmentDependencyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	_q.withIncomingDependencies = query
	return _q
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.Commitment.Query().
//		GroupBy(commitment.FieldCreatedAt).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (_q *CommitmentQuery) GroupBy(field string, fields ...string) *CommitmentGroupBy {
	_q.ctx.Fields = append([]string{field}, fields...)
	grbuild := &CommitmentGroupBy{build: _q}
	grbuild.flds = &_q.ctx.Fields
	grbuild.label = commitment.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CreatedAt time.Time `json:"created_at,omitempty"`
//	}
//
//	client.Commitment.Query().
//		Select(commitment.FieldCreatedAt).
//		Scan(ctx, &v)
func (_q *CommitmentQuery) Select(fields ...string) *CommitmentSelect {
	_q.ctx.Fields = append(_q.ctx.Fields, fields...)
	sbuild := &CommitmentSelect{CommitmentQuery: _q}
	sbuild.label = commitment.Label
	sbuild.flds, sbuild.scan = &_q.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a CommitmentSelect configured with the given aggregations.
func (_q *CommitmentQuery) Aggregate(fns ...AggregateFunc) *CommitmentSelect {
	return _q.Select().Aggregate(fns...)
}

func (_q *CommitmentQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range _q.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, _q); err != nil {
				return err
			}
		}
	}
	for _, f := range _q.ctx.Fields {
		if !commitment.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if _q.path != nil {
		prev, err := _q.path(ctx)
		if err != nil {
			return err
		}
		_q.sql = prev
	}
	return nil
}

func (_q *CommitmentQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*Commitment, error) {
	var (
		nodes       = []*Commitment{}
		withFKs     = _q.withFKs
		_spec       = _q.querySpec()
		loadedTypes = [7]bool{
			_q.withWorkspace != nil,
			_q.withRelationship != nil,
			_q.withUser != nil,
			_q.withEvidences != nil,
			_q.withEvents != nil,
			_q.withOutgoingDependencies != nil,
			_q.withIncomingDependencies != nil,
		}
	)
	if _q.withWorkspace != nil || _q.withRelationship != nil || _q.withUser != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, commitment.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*Commitment).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &Commitment{config: _q.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, _q.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := _q.withWorkspace; query != nil {
		if err := _q.loadWorkspace(ctx, query, nodes, nil,
			func(n *Commitment, e *RevenueWorkspace) { n.Edges.Workspace = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withRelationship; query != nil {
		if err := _q.loadRelationship(ctx, query, nodes, nil,
			func(n *Commitment, e *Relationship) { n.Edges.Relationship = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withUser; query != nil {
		if err := _q.loadUser(ctx, query, nodes, nil,
			func(n *Commitment, e *User) { n.Edges.User = e }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEvidences; query != nil {
		if err := _q.loadEvidences(ctx, query, nodes,
			func(n *Commitment) { n.Edges.Evidences = []*RevenueEvidence{} },
			func(n *Commitment, e *RevenueEvidence) { n.Edges.Evidences = append(n.Edges.Evidences, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withEvents; query != nil {
		if err := _q.loadEvents(ctx, query, nodes,
			func(n *Commitment) { n.Edges.Events = []*CommitmentEvent{} },
			func(n *Commitment, e *CommitmentEvent) { n.Edges.Events = append(n.Edges.Events, e) }); err != nil {
			return nil, err
		}
	}
	if query := _q.withOutgoingDependencies; query != nil {
		if err := _q.loadOutgoingDependencies(ctx, query, nodes,
			func(n *Commitment) { n.Edges.OutgoingDependencies = []*CommitmentDependency{} },
			func(n *Commitment, e *CommitmentDependency) {
				n.Edges.OutgoingDependencies = append(n.Edges.OutgoingDependencies, e)
			}); err != nil {
			return nil, err
		}
	}
	if query := _q.withIncomingDependencies; query != nil {
		if err := _q.loadIncomingDependencies(ctx, query, nodes,
			func(n *Commitment) { n.Edges.IncomingDependencies = []*CommitmentDependency{} },
			func(n *Commitment, e *CommitmentDependency) {
				n.Edges.IncomingDependencies = append(n.Edges.IncomingDependencies, e)
			}); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedEvidences {
		if err := _q.loadEvidences(ctx, query, nodes,
			func(n *Commitment) { n.appendNamedEvidences(name) },
			func(n *Commitment, e *RevenueEvidence) { n.appendNamedEvidences(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedEvents {
		if err := _q.loadEvents(ctx, query, nodes,
			func(n *Commitment) { n.appendNamedEvents(name) },
			func(n *Commitment, e *CommitmentEvent) { n.appendNamedEvents(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedOutgoingDependencies {
		if err := _q.loadOutgoingDependencies(ctx, query, nodes,
			func(n *Commitment) { n.appendNamedOutgoingDependencies(name) },
			func(n *Commitment, e *CommitmentDependency) { n.appendNamedOutgoingDependencies(name, e) }); err != nil {
			return nil, err
		}
	}
	for name, query := range _q.withNamedIncomingDependencies {
		if err := _q.loadIncomingDependencies(ctx, query, nodes,
			func(n *Commitment) { n.appendNamedIncomingDependencies(name) },
			func(n *Commitment, e *CommitmentDependency) { n.appendNamedIncomingDependencies(name, e) }); err != nil {
			return nil, err
		}
	}
	for i := range _q.loadTotal {
		if err := _q.loadTotal[i](ctx, nodes); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (_q *CommitmentQuery) loadWorkspace(ctx context.Context, query *RevenueWorkspaceQuery, nodes []*Commitment, init func(*Commitment), assign func(*Commitment, *RevenueWorkspace)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Commitment)
	for i := range nodes {
		if nodes[i].revenue_workspace_id == nil {
			continue
		}
		fk := *nodes[i].revenue_workspace_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(revenueworkspace.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "revenue_workspace_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CommitmentQuery) loadRelationship(ctx context.Context, query *RelationshipQuery, nodes []*Commitment, init func(*Commitment), assign func(*Commitment, *Relationship)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Commitment)
	for i := range nodes {
		if nodes[i].relationship_id == nil {
			continue
		}
		fk := *nodes[i].relationship_id
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(relationship.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "relationship_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CommitmentQuery) loadUser(ctx context.Context, query *UserQuery, nodes []*Commitment, init func(*Commitment), assign func(*Commitment, *User)) error {
	ids := make([]uuid.UUID, 0, len(nodes))
	nodeids := make(map[uuid.UUID][]*Commitment)
	for i := range nodes {
		if nodes[i].user_commitments == nil {
			continue
		}
		fk := *nodes[i].user_commitments
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "user_commitments" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (_q *CommitmentQuery) loadEvidences(ctx context.Context, query *RevenueEvidenceQuery, nodes []*Commitment, init func(*Commitment), assign func(*Commitment, *RevenueEvidence)) error {
	edgeIDs := make([]driver.Value, len(nodes))
	byID := make(map[uuid.UUID]*Commitment)
	nids := make(map[uuid.UUID]map[*Commitment]struct{})
	for i, node := range nodes {
		edgeIDs[i] = node.ID
		byID[node.ID] = node
		if init != nil {
			init(node)
		}
	}
	query.Where(func(s *sql.Selector) {
		joinT := sql.Table(commitment.EvidencesTable)
		s.Join(joinT).On(s.C(revenueevidence.FieldID), joinT.C(commitment.EvidencesPrimaryKey[1]))
		s.Where(sql.InValues(joinT.C(commitment.EvidencesPrimaryKey[0]), edgeIDs...))
		columns := s.SelectedColumns()
		s.Select(joinT.C(commitment.EvidencesPrimaryKey[0]))
		s.AppendSelect(columns...)
		s.SetDistinct(false)
	})
	if err := query.prepareQuery(ctx); err != nil {
		return err
	}
	qr := QuerierFunc(func(ctx context.Context, q Query) (Value, error) {
		return query.sqlAll(ctx, func(_ context.Context, spec *sqlgraph.QuerySpec) {
			assign := spec.Assign
			values := spec.ScanValues
			spec.ScanValues = func(columns []string) ([]any, error) {
				values, err := values(columns[1:])
				if err != nil {
					return nil, err
				}
				return append([]any{new(uuid.UUID)}, values...), nil
			}
			spec.Assign = func(columns []string, values []any) error {
				outValue := *values[0].(*uuid.UUID)
				inValue := *values[1].(*uuid.UUID)
				if nids[inValue] == nil {
					nids[inValue] = map[*Commitment]struct{}{byID[outValue]: {}}
					return assign(columns[1:], values[1:])
				}
				nids[inValue][byID[outValue]] = struct{}{}
				return nil
			}
		})
	})
	neighbors, err := withInterceptors[[]*RevenueEvidence](ctx, query, qr, query.inters)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected "evidences" node returned %v`, n.ID)
		}
		for kn := range nodes {
			assign(kn, n)
		}
	}
	return nil
}
func (_q *CommitmentQuery) loadEvents(ctx context.Context, query *CommitmentEventQuery, nodes []*Commitment, init func(*Commitment), assign func(*Commitment, *CommitmentEvent)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Commitment)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.CommitmentEvent(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(commitment.EventsColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.commitment_id
		if fk == nil {
			return fmt.Errorf(`foreign-key "commitment_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "commitment_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CommitmentQuery) loadOutgoingDependencies(ctx context.Context, query *CommitmentDependencyQuery, nodes []*Commitment, init func(*Commitment), assign func(*Commitment, *CommitmentDependency)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Commitment)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.CommitmentDependency(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(commitment.OutgoingDependenciesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.from_commitment_id
		if fk == nil {
			return fmt.Errorf(`foreign-key "from_commitment_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "from_commitment_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}
func (_q *CommitmentQuery) loadIncomingDependencies(ctx context.Context, query *CommitmentDependencyQuery, nodes []*Commitment, init func(*Commitment), assign func(*Commitment, *CommitmentDependency)) error {
	fks := make([]driver.Value, 0, len(nodes))
	nodeids := make(map[uuid.UUID]*Commitment)
	for i := range nodes {
		fks = append(fks, nodes[i].ID)
		nodeids[nodes[i].ID] = nodes[i]
		if init != nil {
			init(nodes[i])
		}
	}
	query.withFKs = true
	query.Where(predicate.CommitmentDependency(func(s *sql.Selector) {
		s.Where(sql.InValues(s.C(commitment.IncomingDependenciesColumn), fks...))
	}))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		fk := n.to_commitment_id
		if fk == nil {
			return fmt.Errorf(`foreign-key "to_commitment_id" is nil for node %v`, n.ID)
		}
		node, ok := nodeids[*fk]
		if !ok {
			return fmt.Errorf(`unexpected referenced foreign-key "to_commitment_id" returned %v for node %v`, *fk, n.ID)
		}
		assign(node, n)
	}
	return nil
}

func (_q *CommitmentQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := _q.querySpec()
	if len(_q.modifiers) > 0 {
		_spec.Modifiers = _q.modifiers
	}
	_spec.Node.Columns = _q.ctx.Fields
	if len(_q.ctx.Fields) > 0 {
		_spec.Unique = _q.ctx.Unique != nil && *_q.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, _q.driver, _spec)
}

func (_q *CommitmentQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(commitment.Table, commitment.Columns, sqlgraph.NewFieldSpec(commitment.FieldID, field.TypeUUID))
	_spec.From = _q.sql
	if unique := _q.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if _q.path != nil {
		_spec.Unique = true
	}
	if fields := _q.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, commitment.FieldID)
		for i := range fields {
			if fields[i] != commitment.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := _q.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := _q.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := _q.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := _q.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (_q *CommitmentQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(_q.driver.Dialect())
	t1 := builder.Table(commitment.Table)
	columns := _q.ctx.Fields
	if len(columns) == 0 {
		columns = commitment.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if _q.sql != nil {
		selector = _q.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if _q.ctx.Unique != nil && *_q.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range _q.predicates {
		p(selector)
	}
	for _, p := range _q.order {
		p(selector)
	}
	if offset := _q.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := _q.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// WithNamedEvidences tells the query-builder to eager-load the nodes that are connected to the "evidences"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithNamedEvidences(name string, opts ...func(*RevenueEvidenceQuery)) *CommitmentQuery {
	query := (&RevenueEvidenceClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedEvidences == nil {
		_q.withNamedEvidences = make(map[string]*RevenueEvidenceQuery)
	}
	_q.withNamedEvidences[name] = query
	return _q
}

// WithNamedEvents tells the query-builder to eager-load the nodes that are connected to the "events"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithNamedEvents(name string, opts ...func(*CommitmentEventQuery)) *CommitmentQuery {
	query := (&CommitmentEventClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedEvents == nil {
		_q.withNamedEvents = make(map[string]*CommitmentEventQuery)
	}
	_q.withNamedEvents[name] = query
	return _q
}

// WithNamedOutgoingDependencies tells the query-builder to eager-load the nodes that are connected to the "outgoing_dependencies"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithNamedOutgoingDependencies(name string, opts ...func(*CommitmentDependencyQuery)) *CommitmentQuery {
	query := (&CommitmentDependencyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedOutgoingDependencies == nil {
		_q.withNamedOutgoingDependencies = make(map[string]*CommitmentDependencyQuery)
	}
	_q.withNamedOutgoingDependencies[name] = query
	return _q
}

// WithNamedIncomingDependencies tells the query-builder to eager-load the nodes that are connected to the "incoming_dependencies"
// edge with the given name. The optional arguments are used to configure the query builder of the edge.
func (_q *CommitmentQuery) WithNamedIncomingDependencies(name string, opts ...func(*CommitmentDependencyQuery)) *CommitmentQuery {
	query := (&CommitmentDependencyClient{config: _q.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	if _q.withNamedIncomingDependencies == nil {
		_q.withNamedIncomingDependencies = make(map[string]*CommitmentDependencyQuery)
	}
	_q.withNamedIncomingDependencies[name] = query
	return _q
}

// CommitmentGroupBy is the group-by builder for Commitment entities.
type CommitmentGroupBy struct {
	selector
	build *CommitmentQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (_g *CommitmentGroupBy) Aggregate(fns ...AggregateFunc) *CommitmentGroupBy {
	_g.fns = append(_g.fns, fns...)
	return _g
}

// Scan applies the selector query and scans the result into the given value.
func (_g *CommitmentGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _g.build.ctx, ent.OpQueryGroupBy)
	if err := _g.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CommitmentQuery, *CommitmentGroupBy](ctx, _g.build, _g, _g.build.inters, v)
}

func (_g *CommitmentGroupBy) sqlScan(ctx context.Context, root *CommitmentQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(_g.fns))
	for _, fn := range _g.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*_g.flds)+len(_g.fns))
		for _, f := range *_g.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*_g.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _g.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// CommitmentSelect is the builder for selecting fields of Commitment entities.
type CommitmentSelect struct {
	*CommitmentQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (_s *CommitmentSelect) Aggregate(fns ...AggregateFunc) *CommitmentSelect {
	_s.fns = append(_s.fns, fns...)
	return _s
}

// Scan applies the selector query and scans the result into the given value.
func (_s *CommitmentSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, _s.ctx, ent.OpQuerySelect)
	if err := _s.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*CommitmentQuery, *CommitmentSelect](ctx, _s.CommitmentQuery, _s, _s.inters, v)
}

func (_s *CommitmentSelect) sqlScan(ctx context.Context, root *CommitmentQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(_s.fns))
	for _, fn := range _s.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*_s.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := _s.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

# Electron Forge requires an explicit pnpm hoisting strategy before packaging.
# Keep it narrow: application dependencies are bundled by esbuild and node_modules
# is intentionally excluded from the packaged application.
public-hoist-pattern[]=@electron-forge/*
public-hoist-pattern[]=@electron/*

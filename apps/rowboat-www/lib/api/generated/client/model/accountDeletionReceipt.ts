/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */

/**
 * Response for DELETE /v1/me. Holds no personal data.
 */
export interface AccountDeletionReceipt {
  /** When the deletion finished (RFC 3339). */
  completedAt: string;
  /** Connector grants revoked. */
  connectorsRevoked: number;
  /** Whether the WorkOS identity was deleted. False means support must delete it. */
  identityDeleted: boolean;
  /** Receipt identifier for support. */
  receiptId: string;
  /** When the deletion started (RFC 3339). */
  requestedAt: string;
  /** Stripe subscriptions cancelled. */
  subscriptionsCancelled: number;
  /** Workspaces deleted with the account. */
  workspacesDeleted: number;
  /** Shared workspaces given to another member. */
  workspacesTransferred: number;
}

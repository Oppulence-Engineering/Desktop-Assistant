/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RelationshipAttentionItemRankFactors } from "./relationshipAttentionItemRankFactors";
import type { RelationshipAttentionItemStatus } from "./relationshipAttentionItemStatus";
import type { RelationshipAttentionItemUrgencyBand } from "./relationshipAttentionItemUrgencyBand";

/**
 * Versioned relationship-native reason for portfolio attention.
 */
export interface RelationshipAttentionItem {
  /**
   * Acknowledged time.
   * @nullable
   */
  acknowledgedAt?: string | null;
  /** Acknowledging actor. */
  acknowledgedBy?: string;
  /** Created time. */
  createdAt: string;
  /** Detector version. */
  detectorVersion: number;
  /**
   * Dismissed time.
   * @nullable
   */
  dismissedAt?: string | null;
  /** Dismissing actor. */
  dismissedBy?: string;
  /** Evidence refs. */
  evidenceRefs: string[];
  /**
   * Expiry time.
   * @nullable
   */
  expiresAt?: string | null;
  /** Readable explanation. */
  explanation: string;
  /** Stable UUID primary key. */
  id: string;
  /** Owner id. */
  ownerId?: string;
  /** Projector version. */
  projectorVersion: number;
  /** Readable factor contributions. */
  rankFactors: RelationshipAttentionItemRankFactors;
  /** Internal deterministic rank. */
  rankScore: number;
  /** Detector reason. */
  reasonCode: string;
  /** Recommendation id. */
  recommendationId?: string;
  /** Recommendation revision. */
  recommendationRevision?: number;
  /** Relationship id. */
  relationshipId: string;
  /** Relationship name. */
  relationshipName: string;
  /** Relationship version evaluated. */
  relationshipStateVersion: number;
  /**
   * Snooze time.
   * @nullable
   */
  snoozedUntil?: string | null;
  /** Fresh sources required. */
  sourceRequirements: string[];
  /** Triage reason. */
  stateReason?: string;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: RelationshipAttentionItemStatus;
  /** Triggering object. */
  triggeringObjectRef: string;
  /** Updated time. */
  updatedAt: string;
  /** Urgency. */
  urgencyBand: RelationshipAttentionItemUrgencyBand;
  /** Optimistic version. */
  version: number;
}

/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { BackgroundTaskCreateRequestExecutionTarget } from "./backgroundTaskCreateRequestExecutionTarget";

/**
 * Creates or first-syncs a desktop background task into the cloud mirror.
 */
export interface BackgroundTaskCreateRequest {
  /** Whether this task is active. Defaults to true. */
  active?: boolean;
  /**
   * Original desktop task creation timestamp.
   * @nullable
   */
  createdAt?: string | null;
  /** Where this task should execute. Defaults to desktop. */
  executionTarget?: BackgroundTaskCreateRequestExecutionTarget;
  /** Task instructions mirrored from task.yaml. */
  instructions: string;
  /**
   * Last attempt timestamp from local state.
   * @nullable
   */
  lastAttemptAt?: string | null;
  /**
   * Last local run timestamp.
   * @nullable
   */
  lastRunAt?: string | null;
  /**
   * Latest local run error.
   * @nullable
   */
  lastRunError?: string | null;
  /**
   * Last local run id from the desktop.
   * @nullable
   */
  lastRunId?: string | null;
  /**
   * Latest local run summary.
   * @nullable
   */
  lastRunSummary?: string | null;
  /**
   * Desktop-facing LLM model id.
   * @nullable
   */
  model?: string | null;
  /** Human-readable task name. */
  name: string;
  /**
   * Provider slug. Depending on the row this may be an OAuth provider, LLM provider, or execution backend.
   * @nullable
   */
  provider?: string | null;
  /**
   * Optional stable slug. If omitted, rowboat-api slugifies name.
   * @nullable
   */
  slug?: string | null;
  /** Task trigger configuration mirrored from the desktop task.yaml. Common shapes include cron schedules, window schedules, or event subscriptions. Null clears the mirrored trigger config on PATCH. */
  triggers?: unknown | null;
}

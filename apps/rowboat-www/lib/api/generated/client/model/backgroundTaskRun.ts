/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { BackgroundTaskRunExecutor } from "./backgroundTaskRunExecutor";
import type { BackgroundTaskRunStatus } from "./backgroundTaskRunStatus";
import type { BackgroundTaskRunTrigger } from "./backgroundTaskRunTrigger";

/**
 * Mirrored run state for one desktop background task execution or queued remote trigger.
 */
export interface BackgroundTaskRun {
  /**
   * Desktop run completion timestamp.
   * @nullable
   */
  completedAt?: string | null;
  /** Server row creation timestamp. */
  createdAt: string;
  /**
   * Run error mirrored from the desktop.
   * @nullable
   */
  error?: string | null;
  /** Execution backend that owns this run. */
  executor: BackgroundTaskRunExecutor;
  /** Stable UUID primary key. */
  id: string;
  /**
   * Latest worker heartbeat/progress timestamp.
   * @nullable
   */
  lastHeartbeatAt?: string | null;
  /**
   * Actual desktop run id once a queued remote trigger has been claimed and executed locally.
   * @nullable
   */
  localRunId?: string | null;
  /**
   * Desktop-facing LLM model id.
   * @nullable
   */
  model?: string | null;
  /**
   * Previous run id when this run was created by retry.
   * @nullable
   */
  previousRunId?: string | null;
  /**
   * Human-readable progress message for polling clients.
   * @nullable
   */
  progressMessage?: string | null;
  /**
   * Best-known run progress for polling clients, 0-100.
   * @nullable
   */
  progressPercent?: number | null;
  /**
   * Provider slug. Depending on the row this may be an OAuth provider, LLM provider, or execution backend.
   * @nullable
   */
  provider?: string | null;
  /**
   * Optional context supplied when a user remotely triggered the task.
   * @nullable
   */
  requestedContext?: string | null;
  /** Optimistic-lock revision used by write endpoints. */
  revision: number;
  /** Cloud-visible run id. Desktop-created runs can use local ids; remote triggers use remote-trigger-<uuid> until claimed. */
  runId: string;
  /** Stable per-user background task slug matching bg-tasks/<slug> locally. */
  slug: string;
  /**
   * Desktop run start timestamp.
   * @nullable
   */
  startedAt?: string | null;
  /** Background task run lifecycle state. */
  status: BackgroundTaskRunStatus;
  /**
   * Task-specific usage label for cost attribution.
   * @nullable
   */
  subUseCase?: string | null;
  /**
   * Run summary mirrored from the desktop.
   * @nullable
   */
  summary?: string | null;
  /**
   * Timestamp when Temporal execution closed.
   * @nullable
   */
  temporalClosedAt?: string | null;
  /**
   * Temporal run id for the current workflow execution.
   * @nullable
   */
  temporalRunId?: string | null;
  /**
   * Timestamp when Temporal execution started.
   * @nullable
   */
  temporalStartedAt?: string | null;
  /**
   * Last mirrored Temporal status, separate from the product status.
   * @nullable
   */
  temporalStatus?: string | null;
  /**
   * Temporal workflow id for API-worker runs.
   * @nullable
   */
  temporalWorkflowId?: string | null;
  /** Trigger source for a task run. */
  trigger: BackgroundTaskRunTrigger;
  /** Server row update timestamp. */
  updatedAt: string;
  /**
   * High-level usage label for cost attribution.
   * @nullable
   */
  useCase?: string | null;
}

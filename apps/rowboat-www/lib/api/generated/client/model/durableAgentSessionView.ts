/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { DurableAgentSessionViewStatus } from "./durableAgentSessionViewStatus";

/**
 * Durable agent conversation metadata.
 */
export interface DurableAgentSessionView {
  /** Pinned agent slug. */
  agent: string;
  /**
   * Agent definition source.
   * @nullable
   */
  agentSource?: string | null;
  /** Originating channel. */
  channel: string;
  /**
   * Signed continuation handle when configured.
   * @nullable
   */
  continuationToken: string | null;
  /** Cumulative metered cost units. */
  costUnits: number;
  /** Session creation time. */
  createdAt: string;
  /**
   * Terminal error summary.
   * @nullable
   */
  error?: string | null;
  /**
   * Stable terminal error code.
   * @nullable
   */
  errorCode?: string | null;
  /**
   * Most recent activity time.
   * @nullable
   */
  lastActivityAt?: string | null;
  /** Cumulative model calls. */
  llmCalls: number;
  /** Stable session id. */
  sessionId: string;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: DurableAgentSessionViewStatus;
  /**
   * Conversation title.
   * @nullable
   */
  title?: string | null;
  /** Cumulative tool calls. */
  toolCalls: number;
  /** Completed turn count. */
  turns: number;
}

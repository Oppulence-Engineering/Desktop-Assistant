/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { ConversationDeletionReceiptStatus } from "./conversationDeletionReceiptStatus";
import type { ConversationDeletionReceiptTargetsItem } from "./conversationDeletionReceiptTargetsItem";

/**
 * Immutable deletion request and per-target verification state. Pending device or provider targets keep the receipt partial.
 */
export interface ConversationDeletionReceipt {
  /**
   * Time every required target was verified.
   * @nullable
   */
  completedAt?: string | null;
  /** Whether legal hold blocked deletion. */
  legalHold: boolean;
  /** Idempotent request id. */
  receiptId: string;
  /** Request time. */
  requestedAt: string;
  /** Relationship scope. */
  scopeRef: string;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: ConversationDeletionReceiptStatus;
  /** Per-target outcomes. */
  targets: ConversationDeletionReceiptTargetsItem[];
}

/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RelationshipSourceStatusBackfillPhase } from "./relationshipSourceStatusBackfillPhase";
import type { RelationshipSourceStatusCompleteness } from "./relationshipSourceStatusCompleteness";
import type { RelationshipSourceStatusSource } from "./relationshipSourceStatusSource";
import type { RelationshipSourceStatusStatus } from "./relationshipSourceStatusStatus";

/**
 * User-facing authorization, backfill, freshness, repair, revocation, and disconnect state for one stable provider connection. Tokens and raw cursors are never returned.
 */
export interface RelationshipSourceStatus {
  /**
   * Authorization start.
   * @nullable
   */
  authorizationStartedAt?: string | null;
  /**
   * Authorization completion.
   * @nullable
   */
  authorizedAt?: string | null;
  /** Accepted backfill records. */
  backfillCompleted: number;
  /**
   * Backfill completion.
   * @nullable
   */
  backfillCompletedAt?: string | null;
  /** Backfill phase. */
  backfillPhase: RelationshipSourceStatusBackfillPhase;
  /** Known backfill total, zero when unknown. */
  backfillTotal: number;
  /** Impact on relationship completeness. */
  completeness: RelationshipSourceStatusCompleteness;
  /** Stable source connection id. */
  connectionId: string;
  /** Actor who initiated consent. */
  consentingActorId?: string;
  /**
   * User disconnect time.
   * @nullable
   */
  disconnectedAt?: string | null;
  /** Categorical safe error code. */
  errorCode?: string;
  /** Expected live-sync cadence. */
  expectedCadenceSeconds: number;
  /** Currently granted scopes. */
  grantedScopes: string[];
  /** Calculated sync lag. */
  lagSeconds: number;
  /** Bounded, user-safe error summary. */
  lastError?: string;
  /**
   * Most recent failed sync.
   * @nullable
   */
  lastFailedSyncAt?: string | null;
  /**
   * Newest accepted observation.
   * @nullable
   */
  lastObservationAt?: string | null;
  /**
   * Newest provider event observed.
   * @nullable
   */
  lastProviderEventAt?: string | null;
  /**
   * Most recent successful sync.
   * @nullable
   */
  lastSuccessAt?: string | null;
  /**
   * Most recent sync attempt.
   * @nullable
   */
  lastSyncAt?: string | null;
  /** Missing or revoked required scopes. */
  missingScopes: string[];
  /**
   * Next retry.
   * @nullable
   */
  nextRetryAt?: string | null;
  /** Scopes required by enabled capabilities. */
  requiredScopes: string[];
  /** Bounded retry count. */
  retryCount: number;
  /**
   * Provider revocation time.
   * @nullable
   */
  revokedAt?: string | null;
  /** Source provider. */
  source: RelationshipSourceStatusSource;
  /** Provider account or workspace id. */
  sourceAccountId: string;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: RelationshipSourceStatusStatus;
  /**
   * Backfill start.
   * @nullable
   */
  syncStartedAt?: string | null;
}

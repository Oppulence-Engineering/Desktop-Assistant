/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { CreditLedgerReason } from "./creditLedgerReason";
import type { User } from "./user";

/**
 * Append-only credit grant, reservation, settlement, refund, or consumption entry.
 */
export interface CreditLedger {
  /** Credit delta. Negative values consume/reserve credits; positive values grant or refund credits. */
  delta: number;
  /** Stable UUID primary key. */
  id: string;
  /** Reason code for the ledger entry. */
  reason: CreditLedgerReason;
  /** Idempotency and trace anchor for a metered request. */
  request_id: string;
  /** Usage or ledger event timestamp. */
  ts: string;
  /** User that owns this row. */
  user: User;
}

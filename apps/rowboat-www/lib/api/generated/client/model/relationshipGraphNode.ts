/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RelationshipGraphNodeFreshness } from "./relationshipGraphNodeFreshness";
import type { RelationshipGraphNodeKind } from "./relationshipGraphNodeKind";
import type { RelationshipGraphNodeMetadata } from "./relationshipGraphNodeMetadata";

/**
 * A versioned, typed relationship graph node. Meaning is explicit so clients can render status without relying on color alone.
 */
export interface RelationshipGraphNode {
  /** Action approval state. */
  approvalStatus?: string;
  /** Changed state dimensions. */
  changedDimensions: string[];
  /** Whether this state is newer than the viewer's acknowledgement. */
  changedSinceReview: boolean;
  /**
   * @minimum 0
   * @maximum 1
   */
  confidence?: number;
  /**
   * Due time.
   * @nullable
   */
  dueAt?: string | null;
  /** Engagement state. */
  engagement?: string;
  /** Inspectable evidence ids. */
  evidenceRefs: string[];
  /** Action execution state. */
  executionStatus?: string;
  /** Evidence freshness. */
  freshness?: RelationshipGraphNodeFreshness;
  /** Health state. */
  health?: string;
  /** Stable UUID primary key. */
  id: string;
  /** Node kind. */
  kind: RelationshipGraphNodeKind;
  /** Human-readable label. */
  label: string;
  /** Commercial lifecycle. */
  lifecycle?: string;
  /** Kind-specific bounded metadata. */
  metadata: RelationshipGraphNodeMetadata;
  /**
   * Evidence occurrence time.
   * @nullable
   */
  occurredAt?: string | null;
  /** Action policy state. */
  policyStatus?: string;
  /**
   * @minimum 0
   * @maximum 100
   */
  priority?: number;
  /** Primary relationship id when applicable. */
  relationshipId?: string;
  /** All associated relationships, including shared participants. */
  relationshipIds: string[];
  /** Underlying record id used by explicit Open actions. */
  resourceRef?: string;
  /** Participant role. */
  role?: string;
  /** Sentiment state. */
  sentiment?: string;
  /** Evidence source. */
  source?: string;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status?: string;
  /** Evidence-backed summary. */
  summary?: string;
  /**
   * Last material update.
   * @nullable
   */
  updatedAt?: string | null;
}

/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { ResolvedConversationPolicyCapture } from "./resolvedConversationPolicyCapture";
import type { ResolvedConversationPolicyModelRoute } from "./resolvedConversationPolicyModelRoute";

/**
 * Monotonically resolved conversation policy with every contributing layer recorded.
 */
export interface ResolvedConversationPolicy {
  /** Capture rule. */
  capture: ResolvedConversationPolicyCapture;
  /** Whether externally scoped plan sharing is allowed. */
  externalShare: boolean;
  /** Whether required deletion is blocked. */
  legalHold: boolean;
  /** Most permissive model route allowed. */
  modelRoute: ResolvedConversationPolicyModelRoute;
  /** Hash-bound effective policy version. */
  policyVersion: string;
  /** Whether shared evidence publication is allowed. */
  publishEvidence: boolean;
  /** Classes removed at outbound boundaries. */
  redactionClasses: string[];
  /** Resolution time. */
  resolvedAt: string;
  /** Maximum retention in days. */
  retentionDays: number;
  /** Policy layers that contributed. */
  sourceLayerIds: string[];
}

/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { BackgroundTaskTemplateExecutionTarget } from "./backgroundTaskTemplateExecutionTarget";

/**
 * Built-in starter template for creating API-target background tasks with safe defaults.
 */
export interface BackgroundTaskTemplate {
  /** Whether instantiated tasks are active by default. */
  active: boolean;
  /** Short explanation of what the template does. */
  description: string;
  /** Default execution target. */
  executionTarget: BackgroundTaskTemplateExecutionTarget;
  /** Whether the template is installed and maintained automatically by Oppulence. */
  firstParty: boolean;
  /** Default task instructions. */
  instructions: string;
  /**
   * Desktop-facing LLM model id.
   * @nullable
   */
  model?: string | null;
  /** Default task name. */
  name: string;
  /**
   * Provider slug. Depending on the row this may be an OAuth provider, LLM provider, or execution backend.
   * @nullable
   */
  provider?: string | null;
  /** Connectors this template expects for full fidelity. */
  requiredConnectors?: string[];
  /** Stable template slug. */
  slug: string;
  /** Template tags for UI grouping. */
  tags?: string[];
  /** Default task slug used when instantiating this template. */
  taskSlug: string;
  /** Task trigger configuration mirrored from the desktop task.yaml. Common shapes include cron schedules, window schedules, or event subscriptions. Null clears the mirrored trigger config on PATCH. */
  triggers?: unknown | null;
  /** Monotonic product definition version used for safe upgrades. */
  version: number;
}

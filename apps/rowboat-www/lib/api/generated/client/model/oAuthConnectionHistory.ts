/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { OAuthConnectionHistoryOperation } from "./oAuthConnectionHistoryOperation";

/**
 * Audit history for OAuthConnection rows.
 */
export interface OAuthConnectionHistory {
  /** Row creation timestamp. */
  created_at: string;
  credential_generation: number;
  external_account_id?: string;
  /** Timestamp when this history record was written. */
  history_time: string;
  /** Stable UUID primary key. */
  id: string;
  /** Mutation operation that produced this history row. */
  operation: OAuthConnectionHistoryOperation;
  /** Provider slug. Depending on the row this may be an OAuth provider, LLM provider, or execution backend. */
  provider: string;
  /** UUID of the source row represented by a history row. */
  ref?: string;
  refresh_token_present: boolean;
  /** OAuth scopes granted or requested. */
  scopes?: string[];
  /** Last row update timestamp. */
  updated_at: string;
}

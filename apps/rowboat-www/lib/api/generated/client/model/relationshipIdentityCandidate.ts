/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RelationshipIdentityCandidateImpact } from "./relationshipIdentityCandidateImpact";
import type { RelationshipIdentityCandidateStatus } from "./relationshipIdentityCandidateStatus";
import type { RelationshipIdentityDecision } from "./relationshipIdentityDecision";
import type { RelationshipIdentityLineage } from "./relationshipIdentityLineage";
import type { RevenueRelationship } from "./revenueRelationship";

/**
 * Durable, optimistic-versioned exact-anchor ambiguity review.
 */
export interface RelationshipIdentityCandidate {
  /** Exact anchor kind. */
  anchorKind: string;
  /** Redacted anchor preview. */
  anchorPreview?: string;
  /** Provider. */
  anchorProvider?: string;
  /** Candidate kind. */
  candidateType: string;
  /** Conflicting anchors. */
  conflictingAnchors: string[];
  /**
   * Decision time.
   * @nullable
   */
  decidedAt?: string | null;
  /** Resolved decision. */
  decision?: string;
  /** Actor. */
  decisionActorId?: string;
  /** Reason. */
  decisionReason?: string;
  /** Decision history. */
  decisions: RelationshipIdentityDecision[];
  /** Affected evidence count. */
  evidenceCount: number;
  /**
   * Earliest evidence.
   * @nullable
   */
  evidenceFrom?: string | null;
  /** Evidence references. */
  evidenceRefs: string[];
  /**
   * Latest evidence.
   * @nullable
   */
  evidenceTo?: string | null;
  existingRelationship: RevenueRelationship;
  /** Stable UUID primary key. */
  id: string;
  /** Counts and history that would move. */
  impact: RelationshipIdentityCandidateImpact;
  /** Lineage history. */
  lineage: RelationshipIdentityLineage[];
  /** Matching exact anchors. */
  matchingAnchors: string[];
  proposedRelationship: RevenueRelationship;
  /** Advisory confidence. */
  recommendationConfidence: number;
  /** Advisory decision. */
  recommendedDecision: string;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: RelationshipIdentityCandidateStatus;
  /** Optimistic version. */
  version: number;
}

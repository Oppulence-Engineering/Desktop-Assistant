/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { SubscriptionPlan } from "./subscriptionPlan";
import type { User } from "./user";

/**
 * User billing plan, status, trial expiry, Stripe identifiers, and credit grant.
 */
export interface Subscription {
  /** Row creation timestamp. */
  created_at: string;
  /** Stable UUID primary key. */
  id: string;
  /** Billing plan slug. */
  plan: SubscriptionPlan;
  /** Credits granted by the current subscription. */
  sanctioned_credits: number;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: string;
  /** Stripe customer id when billing is backed by Stripe. */
  stripe_customer_id?: string;
  /** Stripe subscription id when billing is backed by Stripe. */
  stripe_subscription_id?: string;
  /**
   * Trial expiry timestamp when the user is trialing.
   * @nullable
   */
  trial_expires_at?: string | null;
  /** Last row update timestamp. */
  updated_at: string;
  /** User that owns this row. */
  user: User;
}

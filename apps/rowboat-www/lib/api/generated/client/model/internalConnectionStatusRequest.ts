/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */

/**
 * Exact binding extracted from one verified connector resource token. Partial selectors are rejected.
 */
export interface InternalConnectionStatusRequest {
  /** OAuth token audience for the connector. */
  audience: string;
  /** Immutable MCPConnection UUID from the token. */
  connection_id: string;
  /** Connector slug. */
  connector: string;
  /** Credential lifecycle generation minted into the token. */
  credential_generation: number;
  /** JWT ID from the verified product token. */
  jti: string;
  /** Immutable grant-time organization. */
  organization_id: string;
  /** WorkOS user id used to resolve bearer tokens into local users. */
  workos_user_id: string;
}

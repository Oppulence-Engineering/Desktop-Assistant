/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RevenueLeakScanMode } from "./revenueLeakScanMode";
import type { RevenueLeakScanStatus } from "./revenueLeakScanStatus";

/**
 * One bounded historical scan over connected sources (Gmail first). Detectors are deterministic; counts, errors, and freshness make runs incremental and auditable.
 */
export interface RevenueLeakScan {
  /** New queue actions created. */
  actionsCreated?: number;
  /** Detector candidates found. */
  candidatesSeen?: number;
  /**
   * Completion time.
   * @nullable
   */
  completedAt?: string | null;
  /** Bounded failure reason. */
  error?: string;
  /** New evidence rows recorded. */
  evidencesCreated?: number;
  /** Stable UUID primary key. */
  id: string;
  /** Historical lookback in days. */
  lookbackDays: number;
  /** Workspace mode at scan time. */
  mode: RevenueLeakScanMode;
  /** New relationships recorded. */
  relationshipsCreated?: number;
  /**
   * Newest source timestamp observed (incremental cursor).
   * @nullable
   */
  sourceFreshnessAt?: string | null;
  /**
   * Start time.
   * @nullable
   */
  startedAt?: string | null;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: RevenueLeakScanStatus;
  /** Threads examined. */
  threadsSeen?: number;
}

/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { ExportCommitment200OneEvidenceItem } from "./exportCommitment200OneEvidenceItem";
import type { ExportCommitment200OneHistoryItem } from "./exportCommitment200OneHistoryItem";

/**
 * Commitment record.
 */
export type ExportCommitment200One = {
  /** Counterparty account. */
  account: string;
  /** Extraction confidence. */
  confidence: number;
  /** Promise counterparty. */
  counterparty?: string;
  /** promised_by_me, promised_by_them, or mutual. */
  direction: string;
  /**
   * Resolved due time.
   * @nullable
   */
  dueAt?: string | null;
  /** Due condition as stated. */
  duePhrase?: string;
  /** Verbatim cited sources. */
  evidence: ExportCommitment200OneEvidenceItem[];
  /** When the record was produced. */
  generatedAt: string;
  /** Ordered state changes. */
  history: ExportCommitment200OneHistoryItem[];
  /** Commitment id. */
  id: string;
  /** Promise owner. */
  owner?: string;
  /** Register state. */
  state: string;
  /** The obligation. */
  text: string;
};

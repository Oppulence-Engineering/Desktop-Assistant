/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { BackgroundTaskExecutionTarget } from "./backgroundTaskExecutionTarget";
import type { BackgroundTaskScheduleSyncState } from "./backgroundTaskScheduleSyncState";

/**
 * Server-readable mirror of one desktop background task spec. Owned by a user and keyed by slug per user.
 */
export interface BackgroundTask {
  /** Whether the background task is active for scheduling and remote trigger pickup. */
  active: boolean;
  /** Original desktop task creation timestamp when known; otherwise the server row creation time. */
  createdAt: string;
  /** Where this task executes. desktop preserves the local-first path; api dispatches to the Temporal-backed API worker. */
  executionTarget: BackgroundTaskExecutionTarget;
  /** Stable UUID primary key. */
  id: string;
  /** Background task instructions mirrored from task.yaml. */
  instructions: string;
  /**
   * Last time the desktop attempted to run this task.
   * @nullable
   */
  lastAttemptAt?: string | null;
  /**
   * Last time the desktop completed or recorded a run for this task.
   * @nullable
   */
  lastRunAt?: string | null;
  /**
   * Latest run error, empty when the latest run did not fail.
   * @nullable
   */
  lastRunError?: string | null;
  /**
   * Last desktop or remote-trigger run id mirrored for this task.
   * @nullable
   */
  lastRunId?: string | null;
  /**
   * Short summary from the latest run.
   * @nullable
   */
  lastRunSummary?: string | null;
  /**
   * Desktop-facing LLM model id.
   * @nullable
   */
  model?: string | null;
  /** Human-readable background task name. */
  name: string;
  /**
   * Provider slug. Depending on the row this may be an OAuth provider, LLM provider, or execution backend.
   * @nullable
   */
  provider?: string | null;
  /** Optimistic-lock revision used by write endpoints. */
  revision: number;
  /**
   * Latest schedule reconciliation error, empty when healthy.
   * @nullable
   */
  scheduleSyncError?: string | null;
  /** Server-owned Temporal schedule reconciliation state. */
  scheduleSyncState: BackgroundTaskScheduleSyncState;
  /**
   * Last successful schedule reconciliation timestamp.
   * @nullable
   */
  scheduleSyncedAt?: string | null;
  /** Stable per-user background task slug matching bg-tasks/<slug> locally. */
  slug: string;
  /** Whether Oppulence owns and upgrades this definition. Managed tasks can be paused but not deleted or edited. */
  systemManaged: boolean;
  /**
   * First-party template that owns this definition. Empty for user-authored tasks.
   * @nullable
   */
  templateSlug?: string | null;
  /** Installed version of the owning first-party template. Zero for user-authored tasks. */
  templateVersion?: number;
  /** Task trigger configuration mirrored from the desktop task.yaml. Common shapes include cron schedules, window schedules, or event subscriptions. Null clears the mirrored trigger config on PATCH. */
  triggers?: unknown | null;
  /** Server timestamp for the last mirrored task update. */
  updatedAt: string;
}

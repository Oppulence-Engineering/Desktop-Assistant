/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { LLMUsageHistoryOperation } from "./lLMUsageHistoryOperation";

/**
 * Audit history for LLMUsage rows.
 */
export interface LLMUsageHistory {
  /** Optional x-solomon-agent-name header captured for cost allocation. */
  agent_name?: string;
  /** Settled credit cost for the request. */
  cost_units: number;
  /** Timestamp when this history record was written. */
  history_time: string;
  /** Stable UUID primary key. */
  id: string;
  /** Input tokens reported by the upstream or estimated by Solomon AI API. */
  input_tokens: number;
  /** Desktop-facing LLM model id. */
  model: string;
  /** Mutation operation that produced this history row. */
  operation: LLMUsageHistoryOperation;
  /** Output tokens reported by the upstream. */
  output_tokens: number;
  /** UUID of the source row represented by a history row. */
  ref?: string;
  /** Idempotency and trace anchor for a metered request. */
  request_id: string;
  /** Optional x-solomon-sub-use-case header captured for cost allocation. */
  sub_use_case?: string;
  /** Usage or ledger event timestamp. */
  ts: string;
  /** Optional x-solomon-use-case header captured for cost allocation. */
  use_case?: string;
}

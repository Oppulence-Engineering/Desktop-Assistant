/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { ConversationClaimKind } from "./conversationClaimKind";

/**
 * A material conversation claim anchored to exact words, time, speaker confidence, and capture caveats.
 */
export interface ConversationClaim {
  /** Capture caveats. */
  captureCaveats: string[];
  /** Claim confidence. */
  confidence: number;
  /** End offset in milliseconds. */
  endMs: number;
  /** Exact supporting transcript words. */
  exactQuote: string;
  /** Stable UUID primary key. */
  id: string;
  /** Claim kind. */
  kind: ConversationClaimKind;
  /** Whether the claim can affect state or action. */
  material: boolean;
  /** Supporting immutable observation. */
  observationId?: string;
  /** Speaker attribution confidence. */
  speakerConfidence: number;
  /** Meeting-scoped speaker id; never a persistent voiceprint. */
  speakerId: string;
  /** Current meeting-scoped speaker label. */
  speakerLabel: string;
  /** Start offset in milliseconds. */
  startMs: number;
  /** Projected state dimension when applicable. */
  stateDimension?: string;
  /** Normalized claim value. */
  value: string;
}

/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RevenueActionActionType } from "./revenueActionActionType";
import type { RevenueActionApprovalStatus } from "./revenueActionApprovalStatus";
import type { RevenueActionChannel } from "./revenueActionChannel";
import type { RevenueActionDetector } from "./revenueActionDetector";
import type { RevenueActionEvidenceItem } from "./revenueActionEvidenceItem";
import type { RevenueActionExecutionMode } from "./revenueActionExecutionMode";
import type { RevenueActionExecutionOwner } from "./revenueActionExecutionOwner";
import type { RevenueActionExecutionStatus } from "./revenueActionExecutionStatus";
import type { RevenueActionPolicyStatus } from "./revenueActionPolicyStatus";
import type { RevenueActionPriorityComponents } from "./revenueActionPriorityComponents";
import type { RevenueActionQueueStatus } from "./revenueActionQueueStatus";
import type { RevenueActionReason } from "./revenueActionReason";
import type { RevenueActionReconciliationStatus } from "./revenueActionReconciliationStatus";

/**
 * One Revenue Action Queue item. State is split into independent dimensions: queue triage, policy preflight, approval, and execution. Every edit creates a new revision and invalidates the previous policy decision and approval.
 */
export interface RevenueAction {
  /** Action type. */
  actionType: RevenueActionActionType;
  /** Approval state, bound to the exact revision and decision. */
  approvalStatus: RevenueActionApprovalStatus;
  /**
   * Approval time.
   * @nullable
   */
  approvedAt?: string | null;
  /** Revision the approval is bound to. */
  approvedRevision?: number;
  /** Delivery channel. */
  channel: RevenueActionChannel;
  /** Creation time. */
  createdAt?: string;
  /** Detector that produced the action. */
  detector: RevenueActionDetector;
  /** Dismissal reason label. */
  dismissReason?: string;
  /**
   * Due time.
   * @nullable
   */
  dueAt?: string | null;
  /** Exact supporting evidence available in the approval UI. */
  evidence: RevenueActionEvidenceItem[];
  /**
   * Execution time.
   * @nullable
   */
  executedAt?: string | null;
  /** Bounded execution error. */
  executionError?: string;
  /** Draft lands in the operator's mailbox; send requires a passed unexpired decision and a linked workspace. */
  executionMode: RevenueActionExecutionMode;
  /** Exactly one owner may execute a revision. */
  executionOwner: RevenueActionExecutionOwner;
  /** Execution state. A lost provider result is ambiguous, never auto-resent. */
  executionStatus: RevenueActionExecutionStatus;
  /** Stable UUID primary key. */
  id: string;
  /** Preflight state. Facade unavailability keeps pending (fail closed). */
  policyStatus: RevenueActionPolicyStatus;
  /** Per-component priority breakdown; every component is stored and shown. */
  priorityComponents?: RevenueActionPriorityComponents;
  /** Explainable priority score (0-100). */
  priorityScore: number;
  /** Proposed message body. */
  proposedMessage?: string;
  /** Proposed email subject. */
  proposedSubject?: string;
  /** Provider message id after execution. */
  providerMessageId?: string;
  /** Provider thread id after execution. */
  providerThreadId?: string;
  /** Operator triage state. */
  queueStatus: RevenueActionQueueStatus;
  /** Reason code for the ledger entry. */
  reason: RevenueActionReason;
  /** Recipient email address. */
  recipientEmail?: string;
  /** Number of bounded provider lookups performed. */
  reconciliationAttempts?: number;
  /**
   * Most recent provider lookup time.
   * @nullable
   */
  reconciliationCheckedAt?: string | null;
  /** Bounded provider lookup error. */
  reconciliationError?: string;
  /**
   * Next scheduled read-only lookup time.
   * @nullable
   */
  reconciliationNextAt?: string | null;
  /** Read-only provider reconciliation state for an ambiguous write. */
  reconciliationStatus?: RevenueActionReconciliationStatus;
  /** Owning relationship id. */
  relationshipId?: string;
  /** Current revision number. */
  revision: number;
  /** Canonical hash of the revision content. */
  revisionHash: string;
  /** Sender account reference. */
  senderAccountRef?: string;
  /**
   * Snooze wake time.
   * @nullable
   */
  snoozedUntil?: string | null;
  /** Last update time. */
  updatedAt?: string;
}

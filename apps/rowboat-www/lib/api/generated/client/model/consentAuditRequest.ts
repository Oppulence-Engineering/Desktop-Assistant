/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { ConsentAuditRequestEvent } from "./consentAuditRequestEvent";
import type { ConsentAuditRequestResult } from "./consentAuditRequestResult";

/**
 * Append-only, replay-safe oauth-consent audit event. event_id is globally unique and conflicting replays are rejected.
 */
export interface ConsentAuditRequest {
  /** OAuth token audience for the connector. */
  audience: string;
  /** Bound Hydra client id. */
  client_id: string;
  /** Bound connector slug. */
  connector_id: string;
  /** Consent UI session id. */
  consent_session_id: string;
  /** request_id returned by pre-consent. */
  context_request_id: string;
  /** Accepted semantic event. */
  event: ConsentAuditRequestEvent;
  /** Globally unique idempotency id. */
  event_id: string;
  /** RFC3339Nano event time. */
  occurred_at: string;
  /** Bounded JSON result object or string. It must not contain credentials. */
  result?: ConsentAuditRequestResult;
  /** OAuth scopes granted or requested. */
  scopes: string[];
  /** Hook contract version. Only version 1 is accepted. */
  version: number;
  /** WorkOS user id used to resolve bearer tokens into local users. */
  workos_user_id: string;
}

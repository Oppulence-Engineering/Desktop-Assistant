/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RevenuePolicyDecisionCrm } from "./revenuePolicyDecisionCrm";
import type { RevenuePolicyDecisionResearch } from "./revenuePolicyDecisionResearch";
import type { RevenuePolicyDecisionStatus } from "./revenuePolicyDecisionStatus";
import type { RevenuePolicyDecisionSuppression } from "./revenuePolicyDecisionSuppression";
import type { RevenuePolicyDecisionVerification } from "./revenuePolicyDecisionVerification";

/**
 * Immutable OutboundConsole preflight decision for one exact action revision. Rowboat snapshots the decision; it never composes one.
 */
export interface RevenuePolicyDecision {
  /** CRM sub-result snapshot; unknown when the connector is unavailable. */
  crm?: RevenuePolicyDecisionCrm;
  /** Evaluation time. */
  evaluatedAt: string;
  /** Decision expiry; expired decisions must be re-evaluated before approval or execution. */
  expiresAt: string;
  /** Stable UUID primary key. */
  id: string;
  /** Bounded reason codes. */
  reasonCodes?: string[];
  /** Research sub-result snapshot. */
  research?: RevenuePolicyDecisionResearch;
  /** Action revision the decision is about. */
  revision: number;
  /** Revision hash the decision is bound to. */
  revisionHash: string;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: RevenuePolicyDecisionStatus;
  /** Suppression sub-result snapshot. */
  suppression?: RevenuePolicyDecisionSuppression;
  /** Verification sub-result snapshot. */
  verification?: RevenuePolicyDecisionVerification;
}

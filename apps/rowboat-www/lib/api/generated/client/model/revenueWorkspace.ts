/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RevenueWorkspaceMode } from "./revenueWorkspaceMode";
import type { RevenueWorkspaceStatus } from "./revenueWorkspaceStatus";

/**
 * Mapping between the Rowboat tenant and the canonical OutboundConsole workspace. Local mode has no link: observation and draft-only execution work while preflight and sends stay disabled.
 */
export interface RevenueWorkspace {
  /** Stable UUID primary key. */
  id: string;
  /**
   * When the link was last verified.
   * @nullable
   */
  lastVerifiedAt?: string | null;
  /** Workspace mode. */
  mode: RevenueWorkspaceMode;
  /** OutboundConsole organization id. */
  outboundOrganizationId?: string;
  /** OutboundConsole workspace id. */
  outboundWorkspaceId?: string;
  /** Whether policy preflight can run (linked and active). */
  preflightAvailable: boolean;
  /** Lifecycle/status slug. Subscription rows use billing states; background task runs use queued/running/succeeded/failed/stopped. */
  status: RevenueWorkspaceStatus;
}

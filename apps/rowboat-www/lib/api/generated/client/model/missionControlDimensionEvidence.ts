/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { MissionControlDimensionEvidenceAuthority } from "./missionControlDimensionEvidenceAuthority";
import type { MissionControlDimensionEvidenceReviewDecision } from "./missionControlDimensionEvidenceReviewDecision";
import type { MissionControlDimensionEvidenceStatus } from "./missionControlDimensionEvidenceStatus";
import type { MissionControlEvidenceReference } from "./missionControlEvidenceReference";

/**
 * Winning typed assertion, authority decision, validity, and evidence for one projected dimension.
 */
export interface MissionControlDimensionEvidence {
  /** Winning assertion id. */
  assertionId?: string;
  /** Assertion source authority. */
  authority?: MissionControlDimensionEvidenceAuthority;
  /** Deterministic ordinal authority rank. */
  authorityRank?: number;
  /** Assertion confidence. */
  confidence?: number;
  /** Projected dimension. */
  dimension: string;
  /** Supporting observations. */
  evidence: MissionControlEvidenceReference[];
  /** Extractor or deterministic rule version. */
  extractorVersion?: string;
  /** Whether supporting sources are fresh and complete. */
  fresh: boolean;
  /** Why support is missing. */
  missingReason?: string;
  /** Minimum compatible projector version. */
  projectorCompatVersion?: number;
  /** Evidence-backed explanation. */
  reason?: string;
  /** Explicit review decision. */
  reviewDecision?: MissionControlDimensionEvidenceReviewDecision;
  /**
   * Explicit review time.
   * @nullable
   */
  reviewedAt?: string | null;
  /** Explicit reviewer when present. */
  reviewerId?: string;
  /** Assertion lifecycle state. */
  status?: MissionControlDimensionEvidenceStatus;
  /** Whether accessible evidence supports the value. */
  supported: boolean;
  /**
   * Assertion validity start.
   * @nullable
   */
  validFrom?: string | null;
  /**
   * Assertion validity end.
   * @nullable
   */
  validTo?: string | null;
  /** Typed projected value. */
  value?: string;
  /** Typed dimension schema version. */
  valueSchemaVersion?: number;
}

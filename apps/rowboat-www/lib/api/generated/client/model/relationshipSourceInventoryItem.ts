/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import type { RelationshipSourceInventoryItemSource } from "./relationshipSourceInventoryItemSource";
import type { RelationshipSourceStatus } from "./relationshipSourceStatus";

/**
 * Guided source card with consent explanation, supported evidence/actions, and every connected account.
 */
export interface RelationshipSourceInventoryItem {
  /** Provider accounts. */
  accounts: RelationshipSourceStatus[];
  /** Approval-gated actions supported. */
  actions: string[];
  /** Managed connection entry path. */
  connectPath: string;
  /** Credential disconnect path. */
  disconnectPath: string;
  /** Provider display name. */
  displayName: string;
  /** Evidence contributed. */
  evidence: string[];
  /** Expected cadence. */
  expectedCadenceSeconds: number;
  /** Progressive read scopes. */
  readScopes: string[];
  /** Why the scopes are requested. */
  scopeExplanation: string;
  /** Source provider. */
  source: RelationshipSourceInventoryItemSource;
  /** Whether reconnect is supported. */
  supportsReconnect: boolean;
  /** Whether resync is supported. */
  supportsResync: boolean;
  /** Progressive action scopes. */
  writeScopes: string[];
}

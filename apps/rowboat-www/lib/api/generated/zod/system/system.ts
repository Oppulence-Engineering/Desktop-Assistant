/**
 * Generated by orval 🍺
 * Do not edit manually.
 * Solomon AI API
 * Solomon AI's desktop API. The API brokers WorkOS sign-in, billing and credit state, OpenAI-compatible LLM calls, vendor proxies, Google OAuth handoff, connector OAuth, internal webhooks, and admin GraphQL. The ent-generated entity models remain in components as schema references; the documented paths below are the routes mounted by cmd/server/wire.go.
 * OpenAPI spec version: 0.1.0
 */
import * as zod from "zod";

/**
 * Returns ok when the HTTP process is alive. This does not prove dependencies are reachable.
 * @summary Liveness probe
 */
export const GetHealthz200Response = zod
  .strictObject({
    status: zod
      .enum(["ok"])
      .describe(
        "Lifecycle\/status slug. Subscription rows use billing states; background task runs use queued\/running\/succeeded\/failed\/stopped.",
      ),
  })
  .describe("Liveness probe response.");

/**
 * Returns this enriched OpenAPI document. Scalar uses this endpoint to render /docs.
 * @summary Download OpenAPI document
 */
export const GetOpenAPI200Response = zod
  .record(zod.string(), zod.unknown())
  .describe("OpenAPI document.");

/**
 * Runs registered readiness checks such as database connectivity and returns ready only when dependencies are usable.
 * @summary Readiness probe
 */
export const GetReadyz200Response = zod
  .strictObject({
    failed: zod
      .string()
      .nullish()
      .describe("Name of the failed readiness check when status is not_ready."),
    status: zod
      .enum(["ready", "not_ready"])
      .describe(
        "Lifecycle\/status slug. Subscription rows use billing states; background task runs use queued\/running\/succeeded\/failed\/stopped.",
      ),
  })
  .describe("Readiness probe response.");

export const GetReadyz503Response = zod
  .strictObject({
    code: zod.string().describe("Stable machine-readable error code."),
    detail: zod.string().optional().describe("Human-readable error detail."),
    instance: zod.string().nullish().describe("Optional occurrence URI."),
    requestId: zod.string().nullish().describe("Request id emitted by the API middleware."),
    status: zod.int().describe("HTTP status code."),
    title: zod.string().describe("Short HTTP-status summary."),
    traceId: zod.string().nullish().describe("OpenTelemetry trace id when tracing is active."),
    type: zod.string().describe("Problem type URI."),
  })
  .describe(
    "RFC 9457 problem details returned by Solomon AI API handlers. code, requestId, and traceId are extension members.",
  );

/**
 * Public endpoint fetched by the desktop before sign-in. Values identify the app origin, OIDC issuer, optional WebSocket API, and static OAuth client id.
 * @summary Fetch desktop bootstrap config
 */
export const GetConfig200Response = zod
  .strictObject({
    appUrl: zod.string().describe("Browser-facing application origin."),
    oauthClientId: zod
      .string()
      .describe(
        "Pre-registered OAuth\/OIDC client id. Empty means the desktop may fall back to dynamic registration.",
      ),
    oidcIssuerUrl: zod.string().describe("OIDC issuer the desktop signs into."),
    supabaseUrl: zod
      .string()
      .describe("Compatibility alias for oidcIssuerUrl used by older desktop builds."),
    websocketApiUrl: zod
      .string()
      .describe("Optional WebSocket API origin. Empty when not configured."),
  })
  .describe("Public bootstrap values consumed by the desktop before sign-in.");

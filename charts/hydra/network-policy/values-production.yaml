deploymentEnvironment: production

egress:
  postgresql:
    # REQUIRED deployment input. Supply the production Hydra PostgreSQL private
    # endpoint CIDR(s) in a separate environment-owned values file.
    cidrs: []

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rowboat-api.fullname" . }}-config
  labels:
    {{- include "rowboat-api.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}

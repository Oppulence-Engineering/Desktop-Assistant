apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "rowboat-www.fullname" . }}-config
  labels:
    {{- include "rowboat-www.labels" . | nindent 4 }}
data:
  {{- range $k, $v := .Values.config }}
  {{ $k }}: {{ $v | quote }}
  {{- end }}
